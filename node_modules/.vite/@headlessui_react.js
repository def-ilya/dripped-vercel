import {
  require_react_dom
} from "./chunk-Z5NAKZ4P.js";
import {
  __toESM,
  require_react
} from "./chunk-4TRVUPCS.js";

// node_modules/@headlessui/react/dist/_virtual/_rollupPluginBabelHelpers.js
function _extends() {
  _extends = Object.assign || function(target) {
    for (var i = 1; i < arguments.length; i++) {
      var source = arguments[i];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends.apply(this, arguments);
}
function _objectWithoutPropertiesLoose(source, excluded) {
  if (source == null)
    return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i;
  for (i = 0; i < sourceKeys.length; i++) {
    key = sourceKeys[i];
    if (excluded.indexOf(key) >= 0)
      continue;
    target[key] = source[key];
  }
  return target;
}
function _unsupportedIterableToArray(o, minLen) {
  if (!o)
    return;
  if (typeof o === "string")
    return _arrayLikeToArray(o, minLen);
  var n = Object.prototype.toString.call(o).slice(8, -1);
  if (n === "Object" && o.constructor)
    n = o.constructor.name;
  if (n === "Map" || n === "Set")
    return Array.from(o);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n))
    return _arrayLikeToArray(o, minLen);
}
function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length)
    len = arr.length;
  for (var i = 0, arr2 = new Array(len); i < len; i++)
    arr2[i] = arr[i];
  return arr2;
}
function _createForOfIteratorHelperLoose(o, allowArrayLike) {
  var it;
  if (typeof Symbol === "undefined" || o[Symbol.iterator] == null) {
    if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") {
      if (it)
        o = it;
      var i = 0;
      return function() {
        if (i >= o.length)
          return {
            done: true
          };
        return {
          done: false,
          value: o[i++]
        };
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  it = o[Symbol.iterator]();
  return it.next.bind(it);
}

// node_modules/@headlessui/react/dist/components/portal/portal.esm.js
var import_react5 = __toESM(require_react());

// node_modules/@headlessui/react/dist/utils/render.esm.js
var import_react = __toESM(require_react());

// node_modules/@headlessui/react/dist/utils/match.esm.js
function match(value, lookup) {
  if (value in lookup) {
    var returnValue = lookup[value];
    for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
      args[_key - 2] = arguments[_key];
    }
    return typeof returnValue === "function" ? returnValue.apply(void 0, args) : returnValue;
  }
  var error = new Error('Tried to handle "' + value + '" but there is no handler defined. Only defined handlers are: ' + Object.keys(lookup).map(function(key) {
    return '"' + key + '"';
  }).join(", ") + ".");
  if (Error.captureStackTrace)
    Error.captureStackTrace(error, match);
  throw error;
}

// node_modules/@headlessui/react/dist/utils/render.esm.js
var Features;
(function(Features3) {
  Features3[Features3["None"] = 0] = "None";
  Features3[Features3["RenderStrategy"] = 1] = "RenderStrategy";
  Features3[Features3["Static"] = 2] = "Static";
})(Features || (Features = {}));
var RenderStrategy;
(function(RenderStrategy2) {
  RenderStrategy2[RenderStrategy2["Unmount"] = 0] = "Unmount";
  RenderStrategy2[RenderStrategy2["Hidden"] = 1] = "Hidden";
})(RenderStrategy || (RenderStrategy = {}));
function render(_ref) {
  var props = _ref.props, slot = _ref.slot, defaultTag = _ref.defaultTag, features = _ref.features, _ref$visible = _ref.visible, visible = _ref$visible === void 0 ? true : _ref$visible, name = _ref.name;
  if (visible)
    return _render(props, slot, defaultTag, name);
  var featureFlags = features != null ? features : Features.None;
  if (featureFlags & Features.Static) {
    var _props$static = props["static"], isStatic = _props$static === void 0 ? false : _props$static, rest = _objectWithoutPropertiesLoose(props, ["static"]);
    if (isStatic)
      return _render(rest, slot, defaultTag, name);
  }
  if (featureFlags & Features.RenderStrategy) {
    var _match;
    var _props$unmount = props.unmount, unmount = _props$unmount === void 0 ? true : _props$unmount, _rest = _objectWithoutPropertiesLoose(props, ["unmount"]);
    var strategy = unmount ? RenderStrategy.Unmount : RenderStrategy.Hidden;
    return match(strategy, (_match = {}, _match[RenderStrategy.Unmount] = function() {
      return null;
    }, _match[RenderStrategy.Hidden] = function() {
      return _render(_extends({}, _rest, {
        hidden: true,
        style: {
          display: "none"
        }
      }), slot, defaultTag, name);
    }, _match));
  }
  return _render(props, slot, defaultTag, name);
}
function _render(props, slot, tag, name) {
  var _ref2;
  if (slot === void 0) {
    slot = {};
  }
  var _omit = omit(props, ["unmount", "static"]), _omit$as = _omit.as, Component = _omit$as === void 0 ? tag : _omit$as, children = _omit.children, _omit$refName = _omit.refName, refName = _omit$refName === void 0 ? "ref" : _omit$refName, passThroughProps = _objectWithoutPropertiesLoose(_omit, ["as", "children", "refName"]);
  var refRelatedProps = props.ref !== void 0 ? (_ref2 = {}, _ref2[refName] = props.ref, _ref2) : {};
  var resolvedChildren = typeof children === "function" ? children(slot) : children;
  if (passThroughProps.className && typeof passThroughProps.className === "function") {
    passThroughProps.className = passThroughProps.className(slot);
  }
  if (Component === import_react.Fragment) {
    if (Object.keys(passThroughProps).length > 0) {
      if (!(0, import_react.isValidElement)(resolvedChildren) || Array.isArray(resolvedChildren) && resolvedChildren.length > 1) {
        throw new Error(['Passing props on "Fragment"!', "", "The current component <" + name + ' /> is rendering a "Fragment".', "However we need to passthrough the following props:", Object.keys(passThroughProps).map(function(line) {
          return "  - " + line;
        }).join("\n"), "", "You can apply a few solutions:", ['Add an `as="..."` prop, to ensure that we render an actual element instead of a "Fragment".', "Render a single element as the child so that we can forward the props onto that element."].map(function(line) {
          return "  - " + line;
        }).join("\n")].join("\n"));
      }
      return (0, import_react.cloneElement)(resolvedChildren, Object.assign({}, mergeEventFunctions(compact(omit(passThroughProps, ["ref"])), resolvedChildren.props, ["onClick"]), refRelatedProps));
    }
  }
  return (0, import_react.createElement)(Component, Object.assign({}, omit(passThroughProps, ["ref"]), Component !== import_react.Fragment && refRelatedProps), resolvedChildren);
}
function mergeEventFunctions(passThroughProps, existingProps, functionsToMerge) {
  var clone = Object.assign({}, passThroughProps);
  var _loop = function _loop2() {
    var func = _step.value;
    if (passThroughProps[func] !== void 0 && existingProps[func] !== void 0) {
      var _Object$assign;
      Object.assign(clone, (_Object$assign = {}, _Object$assign[func] = function(event) {
        if (!event.defaultPrevented)
          passThroughProps[func](event);
        if (!event.defaultPrevented)
          existingProps[func](event);
      }, _Object$assign));
    }
  };
  for (var _iterator = _createForOfIteratorHelperLoose(functionsToMerge), _step; !(_step = _iterator()).done; ) {
    _loop();
  }
  return clone;
}
function forwardRefWithAs(component) {
  var _component$displayNam;
  return Object.assign((0, import_react.forwardRef)(component), {
    displayName: (_component$displayNam = component.displayName) != null ? _component$displayNam : component.name
  });
}
function compact(object) {
  var clone = Object.assign({}, object);
  for (var key in clone) {
    if (clone[key] === void 0)
      delete clone[key];
  }
  return clone;
}
function omit(object, keysToOmit) {
  if (keysToOmit === void 0) {
    keysToOmit = [];
  }
  var clone = Object.assign({}, object);
  for (var _iterator2 = _createForOfIteratorHelperLoose(keysToOmit), _step2; !(_step2 = _iterator2()).done; ) {
    var key = _step2.value;
    if (key in clone)
      delete clone[key];
  }
  return clone;
}

// node_modules/@headlessui/react/dist/hooks/use-iso-morphic-effect.esm.js
var import_react2 = __toESM(require_react());
var useIsoMorphicEffect = typeof window !== "undefined" ? import_react2.useLayoutEffect : import_react2.useEffect;

// node_modules/@headlessui/react/dist/hooks/use-server-handoff-complete.esm.js
var import_react3 = __toESM(require_react());
var state = {
  serverHandoffComplete: false
};
function useServerHandoffComplete() {
  var _useState = (0, import_react3.useState)(state.serverHandoffComplete), serverHandoffComplete = _useState[0], setServerHandoffComplete = _useState[1];
  (0, import_react3.useEffect)(function() {
    if (serverHandoffComplete === true)
      return;
    setServerHandoffComplete(true);
  }, [serverHandoffComplete]);
  (0, import_react3.useEffect)(function() {
    if (state.serverHandoffComplete === false)
      state.serverHandoffComplete = true;
  }, []);
  return serverHandoffComplete;
}

// node_modules/@headlessui/react/dist/components/portal/portal.esm.js
var import_react_dom = __toESM(require_react_dom());

// node_modules/@headlessui/react/dist/internal/portal-force-root.esm.js
var import_react4 = __toESM(require_react());
var ForcePortalRootContext = (0, import_react4.createContext)(false);
function usePortalRoot() {
  return (0, import_react4.useContext)(ForcePortalRootContext);
}
function ForcePortalRoot(props) {
  return import_react4.default.createElement(ForcePortalRootContext.Provider, {
    value: props.force
  }, props.children);
}

// node_modules/@headlessui/react/dist/components/portal/portal.esm.js
function usePortalTarget() {
  var forceInRoot = usePortalRoot();
  var groupTarget = (0, import_react5.useContext)(PortalGroupContext);
  var _useState = (0, import_react5.useState)(function() {
    if (!forceInRoot && groupTarget !== null)
      return null;
    if (typeof window === "undefined")
      return null;
    var existingRoot = document.getElementById("headlessui-portal-root");
    if (existingRoot)
      return existingRoot;
    var root = document.createElement("div");
    root.setAttribute("id", "headlessui-portal-root");
    return document.body.appendChild(root);
  }), target = _useState[0], setTarget = _useState[1];
  (0, import_react5.useEffect)(function() {
    if (target === null)
      return;
    if (!document.body.contains(target)) {
      document.body.appendChild(target);
    }
  }, [target]);
  (0, import_react5.useEffect)(function() {
    if (forceInRoot)
      return;
    if (groupTarget === null)
      return;
    setTarget(groupTarget.current);
  }, [groupTarget, setTarget, forceInRoot]);
  return target;
}
var DEFAULT_PORTAL_TAG = import_react5.Fragment;
function Portal(props) {
  var passthroughProps = props;
  var target = usePortalTarget();
  var _useState2 = (0, import_react5.useState)(function() {
    return typeof window === "undefined" ? null : document.createElement("div");
  }), element = _useState2[0];
  var ready = useServerHandoffComplete();
  useIsoMorphicEffect(function() {
    if (!target)
      return;
    if (!element)
      return;
    target.appendChild(element);
    return function() {
      if (!target)
        return;
      if (!element)
        return;
      target.removeChild(element);
      if (target.childNodes.length <= 0) {
        var _target$parentElement;
        (_target$parentElement = target.parentElement) == null ? void 0 : _target$parentElement.removeChild(target);
      }
    };
  }, [target, element]);
  if (!ready)
    return null;
  return !target || !element ? null : (0, import_react_dom.createPortal)(render({
    props: passthroughProps,
    defaultTag: DEFAULT_PORTAL_TAG,
    name: "Portal"
  }), element);
}
var DEFAULT_GROUP_TAG = import_react5.Fragment;
var PortalGroupContext = (0, import_react5.createContext)(null);
function Group(props) {
  var target = props.target, passthroughProps = _objectWithoutPropertiesLoose(props, ["target"]);
  return import_react5.default.createElement(PortalGroupContext.Provider, {
    value: target
  }, render({
    props: passthroughProps,
    defaultTag: DEFAULT_GROUP_TAG,
    name: "Popover.Group"
  }));
}
Portal.Group = Group;

// node_modules/@headlessui/react/dist/components/dialog/dialog.esm.js
var import_react14 = __toESM(require_react());

// node_modules/@headlessui/react/dist/hooks/use-sync-refs.esm.js
var import_react6 = __toESM(require_react());
function useSyncRefs() {
  for (var _len = arguments.length, refs = new Array(_len), _key = 0; _key < _len; _key++) {
    refs[_key] = arguments[_key];
  }
  var cache = (0, import_react6.useRef)(refs);
  (0, import_react6.useEffect)(function() {
    cache.current = refs;
  }, [refs]);
  return (0, import_react6.useCallback)(function(value) {
    for (var _iterator = _createForOfIteratorHelperLoose(cache.current), _step; !(_step = _iterator()).done; ) {
      var ref = _step.value;
      if (ref == null)
        continue;
      if (typeof ref === "function")
        ref(value);
      else
        ref.current = value;
    }
  }, [cache]);
}

// node_modules/@headlessui/react/dist/components/keyboard.esm.js
var Keys;
(function(Keys2) {
  Keys2["Space"] = " ";
  Keys2["Enter"] = "Enter";
  Keys2["Escape"] = "Escape";
  Keys2["Backspace"] = "Backspace";
  Keys2["ArrowLeft"] = "ArrowLeft";
  Keys2["ArrowUp"] = "ArrowUp";
  Keys2["ArrowRight"] = "ArrowRight";
  Keys2["ArrowDown"] = "ArrowDown";
  Keys2["Home"] = "Home";
  Keys2["End"] = "End";
  Keys2["PageUp"] = "PageUp";
  Keys2["PageDown"] = "PageDown";
  Keys2["Tab"] = "Tab";
})(Keys || (Keys = {}));

// node_modules/@headlessui/react/dist/utils/bugs.esm.js
function isDisabledReactIssue7711(element) {
  var _ref, _parent;
  var parent = element.parentElement;
  var legend = null;
  while (parent && !(parent instanceof HTMLFieldSetElement)) {
    if (parent instanceof HTMLLegendElement)
      legend = parent;
    parent = parent.parentElement;
  }
  var isParentDisabled = (_ref = ((_parent = parent) == null ? void 0 : _parent.getAttribute("disabled")) === "") != null ? _ref : false;
  if (isParentDisabled && isFirstLegend(legend))
    return false;
  return isParentDisabled;
}
function isFirstLegend(element) {
  if (!element)
    return false;
  var previous = element.previousElementSibling;
  while (previous !== null) {
    if (previous instanceof HTMLLegendElement)
      return false;
    previous = previous.previousElementSibling;
  }
  return true;
}

// node_modules/@headlessui/react/dist/hooks/use-id.esm.js
var import_react7 = __toESM(require_react());
var id = 0;
function generateId() {
  return ++id;
}
function useId() {
  var ready = useServerHandoffComplete();
  var _useState = (0, import_react7.useState)(ready ? generateId : null), id2 = _useState[0], setId = _useState[1];
  useIsoMorphicEffect(function() {
    if (id2 === null)
      setId(generateId());
  }, [id2]);
  return id2 != null ? "" + id2 : void 0;
}

// node_modules/@headlessui/react/dist/hooks/use-window-event.esm.js
var import_react8 = __toESM(require_react());
function useWindowEvent(type, listener, options) {
  var listenerRef = (0, import_react8.useRef)(listener);
  listenerRef.current = listener;
  (0, import_react8.useEffect)(function() {
    function handler(event) {
      listenerRef.current.call(window, event);
    }
    window.addEventListener(type, handler, options);
    return function() {
      return window.removeEventListener(type, handler, options);
    };
  }, [type, options]);
}

// node_modules/@headlessui/react/dist/hooks/use-focus-trap.esm.js
var import_react10 = __toESM(require_react());

// node_modules/@headlessui/react/dist/utils/focus-management.esm.js
var focusableSelector = ["[contentEditable=true]", "[tabindex]", "a[href]", "area[href]", "button:not([disabled])", "iframe", "input:not([disabled])", "select:not([disabled])", "textarea:not([disabled])"].map(false ? function(selector) {
  return selector + ":not([tabindex='-1']):not([style*='display: none'])";
} : function(selector) {
  return selector + ":not([tabindex='-1'])";
}).join(",");
var Focus;
(function(Focus3) {
  Focus3[Focus3["First"] = 1] = "First";
  Focus3[Focus3["Previous"] = 2] = "Previous";
  Focus3[Focus3["Next"] = 4] = "Next";
  Focus3[Focus3["Last"] = 8] = "Last";
  Focus3[Focus3["WrapAround"] = 16] = "WrapAround";
  Focus3[Focus3["NoScroll"] = 32] = "NoScroll";
})(Focus || (Focus = {}));
var FocusResult;
(function(FocusResult2) {
  FocusResult2[FocusResult2["Error"] = 0] = "Error";
  FocusResult2[FocusResult2["Overflow"] = 1] = "Overflow";
  FocusResult2[FocusResult2["Success"] = 2] = "Success";
  FocusResult2[FocusResult2["Underflow"] = 3] = "Underflow";
})(FocusResult || (FocusResult = {}));
var Direction;
(function(Direction2) {
  Direction2[Direction2["Previous"] = -1] = "Previous";
  Direction2[Direction2["Next"] = 1] = "Next";
})(Direction || (Direction = {}));
function getFocusableElements(container) {
  if (container === void 0) {
    container = document.body;
  }
  if (container == null)
    return [];
  return Array.from(container.querySelectorAll(focusableSelector));
}
var FocusableMode;
(function(FocusableMode2) {
  FocusableMode2[FocusableMode2["Strict"] = 0] = "Strict";
  FocusableMode2[FocusableMode2["Loose"] = 1] = "Loose";
})(FocusableMode || (FocusableMode = {}));
function isFocusableElement(element, mode) {
  var _match;
  if (mode === void 0) {
    mode = FocusableMode.Strict;
  }
  if (element === document.body)
    return false;
  return match(mode, (_match = {}, _match[FocusableMode.Strict] = function() {
    return element.matches(focusableSelector);
  }, _match[FocusableMode.Loose] = function() {
    var next = element;
    while (next !== null) {
      if (next.matches(focusableSelector))
        return true;
      next = next.parentElement;
    }
    return false;
  }, _match));
}
function focusElement(element) {
  element == null ? void 0 : element.focus({
    preventScroll: true
  });
}
function focusIn(container, focus) {
  var elements = Array.isArray(container) ? container.slice().sort(function(a, b) {
    var position = a.compareDocumentPosition(b);
    if (position & Node.DOCUMENT_POSITION_FOLLOWING)
      return -1;
    if (position & Node.DOCUMENT_POSITION_PRECEDING)
      return 1;
    return 0;
  }) : getFocusableElements(container);
  var active = document.activeElement;
  var direction = function() {
    if (focus & (Focus.First | Focus.Next))
      return Direction.Next;
    if (focus & (Focus.Previous | Focus.Last))
      return Direction.Previous;
    throw new Error("Missing Focus.First, Focus.Previous, Focus.Next or Focus.Last");
  }();
  var startIndex = function() {
    if (focus & Focus.First)
      return 0;
    if (focus & Focus.Previous)
      return Math.max(0, elements.indexOf(active)) - 1;
    if (focus & Focus.Next)
      return Math.max(0, elements.indexOf(active)) + 1;
    if (focus & Focus.Last)
      return elements.length - 1;
    throw new Error("Missing Focus.First, Focus.Previous, Focus.Next or Focus.Last");
  }();
  var focusOptions = focus & Focus.NoScroll ? {
    preventScroll: true
  } : {};
  var offset = 0;
  var total = elements.length;
  var next = void 0;
  do {
    var _next;
    if (offset >= total || offset + total <= 0)
      return FocusResult.Error;
    var nextIdx = startIndex + offset;
    if (focus & Focus.WrapAround) {
      nextIdx = (nextIdx + total) % total;
    } else {
      if (nextIdx < 0)
        return FocusResult.Underflow;
      if (nextIdx >= total)
        return FocusResult.Overflow;
    }
    next = elements[nextIdx];
    (_next = next) == null ? void 0 : _next.focus(focusOptions);
    offset += direction;
  } while (next !== document.activeElement);
  if (!next.hasAttribute("tabindex"))
    next.setAttribute("tabindex", "0");
  return FocusResult.Success;
}

// node_modules/@headlessui/react/dist/hooks/use-is-mounted.esm.js
var import_react9 = __toESM(require_react());
function useIsMounted() {
  var mounted = (0, import_react9.useRef)(false);
  (0, import_react9.useEffect)(function() {
    mounted.current = true;
    return function() {
      mounted.current = false;
    };
  }, []);
  return mounted;
}

// node_modules/@headlessui/react/dist/hooks/use-focus-trap.esm.js
var Features2;
(function(Features3) {
  Features3[Features3["None"] = 1] = "None";
  Features3[Features3["InitialFocus"] = 2] = "InitialFocus";
  Features3[Features3["TabLock"] = 4] = "TabLock";
  Features3[Features3["FocusLock"] = 8] = "FocusLock";
  Features3[Features3["RestoreFocus"] = 16] = "RestoreFocus";
  Features3[Features3["All"] = 30] = "All";
})(Features2 || (Features2 = {}));
function useFocusTrap(container, features, _temp) {
  if (features === void 0) {
    features = Features2.All;
  }
  var _ref = _temp === void 0 ? {} : _temp, initialFocus = _ref.initialFocus, containers = _ref.containers;
  var restoreElement = (0, import_react10.useRef)(typeof window !== "undefined" ? document.activeElement : null);
  var previousActiveElement = (0, import_react10.useRef)(null);
  var mounted = useIsMounted();
  var featuresRestoreFocus = Boolean(features & Features2.RestoreFocus);
  var featuresInitialFocus = Boolean(features & Features2.InitialFocus);
  (0, import_react10.useEffect)(function() {
    if (!featuresRestoreFocus)
      return;
    restoreElement.current = document.activeElement;
  }, [featuresRestoreFocus]);
  (0, import_react10.useEffect)(function() {
    if (!featuresRestoreFocus)
      return;
    return function() {
      focusElement(restoreElement.current);
      restoreElement.current = null;
    };
  }, [featuresRestoreFocus]);
  (0, import_react10.useEffect)(function() {
    if (!featuresInitialFocus)
      return;
    if (!container.current)
      return;
    var activeElement = document.activeElement;
    if (initialFocus == null ? void 0 : initialFocus.current) {
      if ((initialFocus == null ? void 0 : initialFocus.current) === activeElement) {
        previousActiveElement.current = activeElement;
        return;
      }
    } else if (container.current.contains(activeElement)) {
      previousActiveElement.current = activeElement;
      return;
    }
    if (initialFocus == null ? void 0 : initialFocus.current) {
      focusElement(initialFocus.current);
    } else {
      if (focusIn(container.current, Focus.First) === FocusResult.Error) {
        console.warn("There are no focusable elements inside the <FocusTrap />");
      }
    }
    previousActiveElement.current = document.activeElement;
  }, [container, initialFocus, featuresInitialFocus]);
  useWindowEvent("keydown", function(event) {
    if (!(features & Features2.TabLock))
      return;
    if (!container.current)
      return;
    if (event.key !== Keys.Tab)
      return;
    event.preventDefault();
    if (focusIn(container.current, (event.shiftKey ? Focus.Previous : Focus.Next) | Focus.WrapAround) === FocusResult.Success) {
      previousActiveElement.current = document.activeElement;
    }
  });
  useWindowEvent("focus", function(event) {
    if (!(features & Features2.FocusLock))
      return;
    var allContainers = new Set(containers == null ? void 0 : containers.current);
    allContainers.add(container);
    if (!allContainers.size)
      return;
    var previous = previousActiveElement.current;
    if (!previous)
      return;
    if (!mounted.current)
      return;
    var toElement = event.target;
    if (toElement && toElement instanceof HTMLElement) {
      if (!contains(allContainers, toElement)) {
        event.preventDefault();
        event.stopPropagation();
        focusElement(previous);
      } else {
        previousActiveElement.current = toElement;
        focusElement(toElement);
      }
    } else {
      focusElement(previousActiveElement.current);
    }
  }, true);
}
function contains(containers, element) {
  for (var _iterator = _createForOfIteratorHelperLoose(containers), _step; !(_step = _iterator()).done; ) {
    var _container$current;
    var container = _step.value;
    if ((_container$current = container.current) == null ? void 0 : _container$current.contains(element))
      return true;
  }
  return false;
}

// node_modules/@headlessui/react/dist/hooks/use-inert-others.esm.js
var interactables = /* @__PURE__ */ new Set();
var originals = /* @__PURE__ */ new Map();
function inert(element) {
  element.setAttribute("aria-hidden", "true");
  element.inert = true;
}
function restore(element) {
  var original = originals.get(element);
  if (!original)
    return;
  if (original["aria-hidden"] === null)
    element.removeAttribute("aria-hidden");
  else
    element.setAttribute("aria-hidden", original["aria-hidden"]);
  element.inert = original.inert;
}
function useInertOthers(container, enabled) {
  if (enabled === void 0) {
    enabled = true;
  }
  useIsoMorphicEffect(function() {
    if (!enabled)
      return;
    if (!container.current)
      return;
    var element = container.current;
    interactables.add(element);
    for (var _iterator = _createForOfIteratorHelperLoose(originals.keys()), _step; !(_step = _iterator()).done; ) {
      var original = _step.value;
      if (original.contains(element)) {
        restore(original);
        originals["delete"](original);
      }
    }
    document.querySelectorAll("body > *").forEach(function(child) {
      if (!(child instanceof HTMLElement))
        return;
      for (var _iterator2 = _createForOfIteratorHelperLoose(interactables), _step2; !(_step2 = _iterator2()).done; ) {
        var interactable = _step2.value;
        if (child.contains(interactable))
          return;
      }
      if (interactables.size === 1) {
        originals.set(child, {
          "aria-hidden": child.getAttribute("aria-hidden"),
          inert: child.inert
        });
        inert(child);
      }
    });
    return function() {
      interactables["delete"](element);
      if (interactables.size > 0) {
        document.querySelectorAll("body > *").forEach(function(child) {
          if (!(child instanceof HTMLElement))
            return;
          if (originals.has(child))
            return;
          for (var _iterator3 = _createForOfIteratorHelperLoose(interactables), _step3; !(_step3 = _iterator3()).done; ) {
            var interactable = _step3.value;
            if (child.contains(interactable))
              return;
          }
          originals.set(child, {
            "aria-hidden": child.getAttribute("aria-hidden"),
            inert: child.inert
          });
          inert(child);
        });
      } else {
        for (var _iterator4 = _createForOfIteratorHelperLoose(originals.keys()), _step4; !(_step4 = _iterator4()).done; ) {
          var _element = _step4.value;
          restore(_element);
          originals["delete"](_element);
        }
      }
    };
  }, [enabled]);
}

// node_modules/@headlessui/react/dist/components/description/description.esm.js
var import_react11 = __toESM(require_react());
var DescriptionContext = (0, import_react11.createContext)(null);
function useDescriptionContext() {
  var context = (0, import_react11.useContext)(DescriptionContext);
  if (context === null) {
    var err = new Error("You used a <Description /> component, but it is not inside a relevant parent.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useDescriptionContext);
    throw err;
  }
  return context;
}
function useDescriptions() {
  var _useState = (0, import_react11.useState)([]), descriptionIds = _useState[0], setDescriptionIds = _useState[1];
  return [
    descriptionIds.length > 0 ? descriptionIds.join(" ") : void 0,
    (0, import_react11.useMemo)(function() {
      return function DescriptionProvider(props) {
        var register = (0, import_react11.useCallback)(function(value) {
          setDescriptionIds(function(existing) {
            return [].concat(existing, [value]);
          });
          return function() {
            return setDescriptionIds(function(existing) {
              var clone = existing.slice();
              var idx = clone.indexOf(value);
              if (idx !== -1)
                clone.splice(idx, 1);
              return clone;
            });
          };
        }, []);
        var contextBag = (0, import_react11.useMemo)(function() {
          return {
            register,
            slot: props.slot,
            name: props.name,
            props: props.props
          };
        }, [register, props.slot, props.name, props.props]);
        return import_react11.default.createElement(DescriptionContext.Provider, {
          value: contextBag
        }, props.children);
      };
    }, [setDescriptionIds])
  ];
}
var DEFAULT_DESCRIPTION_TAG = "p";
function Description(props) {
  var context = useDescriptionContext();
  var id2 = "headlessui-description-" + useId();
  useIsoMorphicEffect(function() {
    return context.register(id2);
  }, [id2, context.register]);
  var passThroughProps = props;
  var propsWeControl = _extends({}, context.props, {
    id: id2
  });
  return render({
    props: _extends({}, passThroughProps, propsWeControl),
    slot: context.slot || {},
    defaultTag: DEFAULT_DESCRIPTION_TAG,
    name: context.name || "Description"
  });
}

// node_modules/@headlessui/react/dist/internal/open-closed.esm.js
var import_react12 = __toESM(require_react());
var Context = (0, import_react12.createContext)(null);
Context.displayName = "OpenClosedContext";
var State;
(function(State2) {
  State2[State2["Open"] = 0] = "Open";
  State2[State2["Closed"] = 1] = "Closed";
})(State || (State = {}));
function useOpenClosed() {
  return (0, import_react12.useContext)(Context);
}
function OpenClosedProvider(_ref) {
  var value = _ref.value, children = _ref.children;
  return import_react12.default.createElement(Context.Provider, {
    value
  }, children);
}

// node_modules/@headlessui/react/dist/internal/stack-context.esm.js
var import_react13 = __toESM(require_react());
var StackContext = (0, import_react13.createContext)(function() {
});
StackContext.displayName = "StackContext";
var StackMessage;
(function(StackMessage2) {
  StackMessage2[StackMessage2["Add"] = 0] = "Add";
  StackMessage2[StackMessage2["Remove"] = 1] = "Remove";
})(StackMessage || (StackMessage = {}));
function useStackContext() {
  return (0, import_react13.useContext)(StackContext);
}
function StackProvider(_ref) {
  var children = _ref.children, onUpdate = _ref.onUpdate, type = _ref.type, element = _ref.element;
  var parentUpdate = useStackContext();
  var notify = (0, import_react13.useCallback)(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    onUpdate == null ? void 0 : onUpdate.apply(void 0, args);
    parentUpdate.apply(void 0, args);
  }, [parentUpdate, onUpdate]);
  useIsoMorphicEffect(function() {
    notify(StackMessage.Add, type, element);
    return function() {
      return notify(StackMessage.Remove, type, element);
    };
  }, [notify, type, element]);
  return import_react13.default.createElement(StackContext.Provider, {
    value: notify
  }, children);
}

// node_modules/@headlessui/react/dist/components/dialog/dialog.esm.js
var _reducers;
var DialogStates;
(function(DialogStates2) {
  DialogStates2[DialogStates2["Open"] = 0] = "Open";
  DialogStates2[DialogStates2["Closed"] = 1] = "Closed";
})(DialogStates || (DialogStates = {}));
var ActionTypes;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["SetTitleId"] = 0] = "SetTitleId";
})(ActionTypes || (ActionTypes = {}));
var reducers = (_reducers = {}, _reducers[ActionTypes.SetTitleId] = function(state2, action) {
  if (state2.titleId === action.id)
    return state2;
  return _extends({}, state2, {
    titleId: action.id
  });
}, _reducers);
var DialogContext = (0, import_react14.createContext)(null);
DialogContext.displayName = "DialogContext";
function useDialogContext(component) {
  var context = (0, import_react14.useContext)(DialogContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Dialog2.displayName + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useDialogContext);
    throw err;
  }
  return context;
}
function stateReducer(state2, action) {
  return match(action.type, reducers, state2, action);
}
var DEFAULT_DIALOG_TAG = "div";
var DialogRenderFeatures = Features.RenderStrategy | Features.Static;
var DialogRoot = forwardRefWithAs(function Dialog(props, ref) {
  var open = props.open, onClose = props.onClose, initialFocus = props.initialFocus, rest = _objectWithoutPropertiesLoose(props, ["open", "onClose", "initialFocus"]);
  var _useState = (0, import_react14.useState)(0), nestedDialogCount = _useState[0], setNestedDialogCount = _useState[1];
  var usesOpenClosedState = useOpenClosed();
  if (open === void 0 && usesOpenClosedState !== null) {
    var _match;
    open = match(usesOpenClosedState, (_match = {}, _match[State.Open] = true, _match[State.Closed] = false, _match));
  }
  var containers = (0, import_react14.useRef)(/* @__PURE__ */ new Set());
  var internalDialogRef = (0, import_react14.useRef)(null);
  var dialogRef = useSyncRefs(internalDialogRef, ref);
  var hasOpen = props.hasOwnProperty("open") || usesOpenClosedState !== null;
  var hasOnClose = props.hasOwnProperty("onClose");
  if (!hasOpen && !hasOnClose) {
    throw new Error("You have to provide an `open` and an `onClose` prop to the `Dialog` component.");
  }
  if (!hasOpen) {
    throw new Error("You provided an `onClose` prop to the `Dialog`, but forgot an `open` prop.");
  }
  if (!hasOnClose) {
    throw new Error("You provided an `open` prop to the `Dialog`, but forgot an `onClose` prop.");
  }
  if (typeof open !== "boolean") {
    throw new Error("You provided an `open` prop to the `Dialog`, but the value is not a boolean. Received: " + open);
  }
  if (typeof onClose !== "function") {
    throw new Error("You provided an `onClose` prop to the `Dialog`, but the value is not a function. Received: " + onClose);
  }
  var dialogState = open ? DialogStates.Open : DialogStates.Closed;
  var visible = function() {
    if (usesOpenClosedState !== null) {
      return usesOpenClosedState === State.Open;
    }
    return dialogState === DialogStates.Open;
  }();
  var _useReducer = (0, import_react14.useReducer)(stateReducer, {
    titleId: null,
    descriptionId: null
  }), state2 = _useReducer[0], dispatch = _useReducer[1];
  var close = (0, import_react14.useCallback)(function() {
    return onClose(false);
  }, [onClose]);
  var setTitleId = (0, import_react14.useCallback)(function(id3) {
    return dispatch({
      type: ActionTypes.SetTitleId,
      id: id3
    });
  }, [dispatch]);
  var ready = useServerHandoffComplete();
  var enabled = ready && dialogState === DialogStates.Open;
  var hasNestedDialogs = nestedDialogCount > 1;
  var hasParentDialog = (0, import_react14.useContext)(DialogContext) !== null;
  var position = !hasNestedDialogs ? "leaf" : "parent";
  useFocusTrap(internalDialogRef, enabled ? match(position, {
    parent: Features2.RestoreFocus,
    leaf: Features2.All
  }) : Features2.None, {
    initialFocus,
    containers
  });
  useInertOthers(internalDialogRef, hasNestedDialogs ? enabled : false);
  useWindowEvent("mousedown", function(event) {
    var _internalDialogRef$cu;
    var target = event.target;
    if (dialogState !== DialogStates.Open)
      return;
    if (hasNestedDialogs)
      return;
    if ((_internalDialogRef$cu = internalDialogRef.current) == null ? void 0 : _internalDialogRef$cu.contains(target))
      return;
    close();
  });
  useWindowEvent("keydown", function(event) {
    if (event.key !== Keys.Escape)
      return;
    if (dialogState !== DialogStates.Open)
      return;
    if (hasNestedDialogs)
      return;
    event.preventDefault();
    event.stopPropagation();
    close();
  });
  (0, import_react14.useEffect)(function() {
    if (dialogState !== DialogStates.Open)
      return;
    if (hasParentDialog)
      return;
    var overflow = document.documentElement.style.overflow;
    var paddingRight = document.documentElement.style.paddingRight;
    var scrollbarWidth = window.innerWidth - document.documentElement.clientWidth;
    document.documentElement.style.overflow = "hidden";
    document.documentElement.style.paddingRight = scrollbarWidth + "px";
    return function() {
      document.documentElement.style.overflow = overflow;
      document.documentElement.style.paddingRight = paddingRight;
    };
  }, [dialogState, hasParentDialog]);
  (0, import_react14.useEffect)(function() {
    if (dialogState !== DialogStates.Open)
      return;
    if (!internalDialogRef.current)
      return;
    var observer = new IntersectionObserver(function(entries) {
      for (var _iterator = _createForOfIteratorHelperLoose(entries), _step; !(_step = _iterator()).done; ) {
        var entry = _step.value;
        if (entry.boundingClientRect.x === 0 && entry.boundingClientRect.y === 0 && entry.boundingClientRect.width === 0 && entry.boundingClientRect.height === 0) {
          close();
        }
      }
    });
    observer.observe(internalDialogRef.current);
    return function() {
      return observer.disconnect();
    };
  }, [dialogState, internalDialogRef, close]);
  var _useDescriptions = useDescriptions(), describedby = _useDescriptions[0], DescriptionProvider = _useDescriptions[1];
  var id2 = "headlessui-dialog-" + useId();
  var contextBag = (0, import_react14.useMemo)(function() {
    return [{
      dialogState,
      close,
      setTitleId
    }, state2];
  }, [dialogState, state2, close, setTitleId]);
  var slot = (0, import_react14.useMemo)(function() {
    return {
      open: dialogState === DialogStates.Open
    };
  }, [dialogState]);
  var propsWeControl = {
    ref: dialogRef,
    id: id2,
    role: "dialog",
    "aria-modal": dialogState === DialogStates.Open ? true : void 0,
    "aria-labelledby": state2.titleId,
    "aria-describedby": describedby,
    onClick: function onClick(event) {
      event.stopPropagation();
    }
  };
  var passthroughProps = rest;
  return import_react14.default.createElement(StackProvider, {
    type: "Dialog",
    element: internalDialogRef,
    onUpdate: (0, import_react14.useCallback)(function(message, type, element) {
      var _match2;
      if (type !== "Dialog")
        return;
      match(message, (_match2 = {}, _match2[StackMessage.Add] = function() {
        containers.current.add(element);
        setNestedDialogCount(function(count) {
          return count + 1;
        });
      }, _match2[StackMessage.Remove] = function() {
        containers.current.add(element);
        setNestedDialogCount(function(count) {
          return count - 1;
        });
      }, _match2));
    }, [])
  }, import_react14.default.createElement(ForcePortalRoot, {
    force: true
  }, import_react14.default.createElement(Portal, null, import_react14.default.createElement(DialogContext.Provider, {
    value: contextBag
  }, import_react14.default.createElement(Portal.Group, {
    target: internalDialogRef
  }, import_react14.default.createElement(ForcePortalRoot, {
    force: false
  }, import_react14.default.createElement(DescriptionProvider, {
    slot,
    name: "Dialog.Description"
  }, render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_DIALOG_TAG,
    features: DialogRenderFeatures,
    visible,
    name: "Dialog"
  }))))))));
});
var DEFAULT_OVERLAY_TAG = "div";
var Overlay = forwardRefWithAs(function Overlay2(props, ref) {
  var _useDialogContext = useDialogContext([Dialog2.displayName, Overlay2.name].join(".")), _useDialogContext$ = _useDialogContext[0], dialogState = _useDialogContext$.dialogState, close = _useDialogContext$.close;
  var overlayRef = useSyncRefs(ref);
  var id2 = "headlessui-dialog-overlay-" + useId();
  var handleClick = (0, import_react14.useCallback)(function(event) {
    if (event.target !== event.currentTarget)
      return;
    if (isDisabledReactIssue7711(event.currentTarget))
      return event.preventDefault();
    event.preventDefault();
    event.stopPropagation();
    close();
  }, [close]);
  var slot = (0, import_react14.useMemo)(function() {
    return {
      open: dialogState === DialogStates.Open
    };
  }, [dialogState]);
  var propsWeControl = {
    ref: overlayRef,
    id: id2,
    "aria-hidden": true,
    onClick: handleClick
  };
  var passthroughProps = props;
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_OVERLAY_TAG,
    name: "Dialog.Overlay"
  });
});
var DEFAULT_TITLE_TAG = "h2";
function Title(props) {
  var _useDialogContext2 = useDialogContext([Dialog2.displayName, Title.name].join(".")), _useDialogContext2$ = _useDialogContext2[0], dialogState = _useDialogContext2$.dialogState, setTitleId = _useDialogContext2$.setTitleId;
  var id2 = "headlessui-dialog-title-" + useId();
  (0, import_react14.useEffect)(function() {
    setTitleId(id2);
    return function() {
      return setTitleId(null);
    };
  }, [id2, setTitleId]);
  var slot = (0, import_react14.useMemo)(function() {
    return {
      open: dialogState === DialogStates.Open
    };
  }, [dialogState]);
  var propsWeControl = {
    id: id2
  };
  var passthroughProps = props;
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_TITLE_TAG,
    name: "Dialog.Title"
  });
}
var Dialog2 = Object.assign(DialogRoot, {
  Overlay,
  Title,
  Description
});

// node_modules/@headlessui/react/dist/components/disclosure/disclosure.esm.js
var import_react16 = __toESM(require_react());

// node_modules/@headlessui/react/dist/hooks/use-resolve-button-type.esm.js
var import_react15 = __toESM(require_react());
function resolveType(props) {
  var _props$as;
  if (props.type)
    return props.type;
  var tag = (_props$as = props.as) != null ? _props$as : "button";
  if (typeof tag === "string" && tag.toLowerCase() === "button")
    return "button";
  return void 0;
}
function useResolveButtonType(props, ref) {
  var _useState = (0, import_react15.useState)(function() {
    return resolveType(props);
  }), type = _useState[0], setType = _useState[1];
  useIsoMorphicEffect(function() {
    setType(resolveType(props));
  }, [props.type, props.as]);
  useIsoMorphicEffect(function() {
    if (type)
      return;
    if (!ref.current)
      return;
    if (ref.current instanceof HTMLButtonElement && !ref.current.hasAttribute("type")) {
      setType("button");
    }
  }, [type, ref]);
  return type;
}

// node_modules/@headlessui/react/dist/components/disclosure/disclosure.esm.js
var _reducers2;
var DisclosureStates;
(function(DisclosureStates2) {
  DisclosureStates2[DisclosureStates2["Open"] = 0] = "Open";
  DisclosureStates2[DisclosureStates2["Closed"] = 1] = "Closed";
})(DisclosureStates || (DisclosureStates = {}));
var ActionTypes2;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["ToggleDisclosure"] = 0] = "ToggleDisclosure";
  ActionTypes8[ActionTypes8["CloseDisclosure"] = 1] = "CloseDisclosure";
  ActionTypes8[ActionTypes8["SetButtonId"] = 2] = "SetButtonId";
  ActionTypes8[ActionTypes8["SetPanelId"] = 3] = "SetPanelId";
  ActionTypes8[ActionTypes8["LinkPanel"] = 4] = "LinkPanel";
  ActionTypes8[ActionTypes8["UnlinkPanel"] = 5] = "UnlinkPanel";
})(ActionTypes2 || (ActionTypes2 = {}));
var reducers2 = (_reducers2 = {}, _reducers2[ActionTypes2.ToggleDisclosure] = function(state2) {
  var _match;
  return _extends({}, state2, {
    disclosureState: match(state2.disclosureState, (_match = {}, _match[DisclosureStates.Open] = DisclosureStates.Closed, _match[DisclosureStates.Closed] = DisclosureStates.Open, _match))
  });
}, _reducers2[ActionTypes2.CloseDisclosure] = function(state2) {
  if (state2.disclosureState === DisclosureStates.Closed)
    return state2;
  return _extends({}, state2, {
    disclosureState: DisclosureStates.Closed
  });
}, _reducers2[ActionTypes2.LinkPanel] = function(state2) {
  if (state2.linkedPanel === true)
    return state2;
  return _extends({}, state2, {
    linkedPanel: true
  });
}, _reducers2[ActionTypes2.UnlinkPanel] = function(state2) {
  if (state2.linkedPanel === false)
    return state2;
  return _extends({}, state2, {
    linkedPanel: false
  });
}, _reducers2[ActionTypes2.SetButtonId] = function(state2, action) {
  if (state2.buttonId === action.buttonId)
    return state2;
  return _extends({}, state2, {
    buttonId: action.buttonId
  });
}, _reducers2[ActionTypes2.SetPanelId] = function(state2, action) {
  if (state2.panelId === action.panelId)
    return state2;
  return _extends({}, state2, {
    panelId: action.panelId
  });
}, _reducers2);
var DisclosureContext = (0, import_react16.createContext)(null);
DisclosureContext.displayName = "DisclosureContext";
function useDisclosureContext(component) {
  var context = (0, import_react16.useContext)(DisclosureContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Disclosure.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useDisclosureContext);
    throw err;
  }
  return context;
}
var DisclosureAPIContext = (0, import_react16.createContext)(null);
DisclosureAPIContext.displayName = "DisclosureAPIContext";
function useDisclosureAPIContext(component) {
  var context = (0, import_react16.useContext)(DisclosureAPIContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Disclosure.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useDisclosureAPIContext);
    throw err;
  }
  return context;
}
var DisclosurePanelContext = (0, import_react16.createContext)(null);
DisclosurePanelContext.displayName = "DisclosurePanelContext";
function useDisclosurePanelContext() {
  return (0, import_react16.useContext)(DisclosurePanelContext);
}
function stateReducer2(state2, action) {
  return match(action.type, reducers2, state2, action);
}
var DEFAULT_DISCLOSURE_TAG = import_react16.Fragment;
function Disclosure(props) {
  var _match2;
  var _props$defaultOpen = props.defaultOpen, defaultOpen = _props$defaultOpen === void 0 ? false : _props$defaultOpen, passthroughProps = _objectWithoutPropertiesLoose(props, ["defaultOpen"]);
  var buttonId = "headlessui-disclosure-button-" + useId();
  var panelId = "headlessui-disclosure-panel-" + useId();
  var reducerBag = (0, import_react16.useReducer)(stateReducer2, {
    disclosureState: defaultOpen ? DisclosureStates.Open : DisclosureStates.Closed,
    linkedPanel: false,
    buttonId,
    panelId
  });
  var disclosureState = reducerBag[0].disclosureState, dispatch = reducerBag[1];
  (0, import_react16.useEffect)(function() {
    return dispatch({
      type: ActionTypes2.SetButtonId,
      buttonId
    });
  }, [buttonId, dispatch]);
  (0, import_react16.useEffect)(function() {
    return dispatch({
      type: ActionTypes2.SetPanelId,
      panelId
    });
  }, [panelId, dispatch]);
  var close = (0, import_react16.useCallback)(function(focusableElement) {
    dispatch({
      type: ActionTypes2.CloseDisclosure
    });
    var restoreElement = function() {
      if (!focusableElement)
        return document.getElementById(buttonId);
      if (focusableElement instanceof HTMLElement)
        return focusableElement;
      if (focusableElement.current instanceof HTMLElement)
        return focusableElement.current;
      return document.getElementById(buttonId);
    }();
    restoreElement == null ? void 0 : restoreElement.focus();
  }, [dispatch, buttonId]);
  var api = (0, import_react16.useMemo)(function() {
    return {
      close
    };
  }, [close]);
  var slot = (0, import_react16.useMemo)(function() {
    return {
      open: disclosureState === DisclosureStates.Open,
      close
    };
  }, [disclosureState, close]);
  return import_react16.default.createElement(DisclosureContext.Provider, {
    value: reducerBag
  }, import_react16.default.createElement(DisclosureAPIContext.Provider, {
    value: api
  }, import_react16.default.createElement(OpenClosedProvider, {
    value: match(disclosureState, (_match2 = {}, _match2[DisclosureStates.Open] = State.Open, _match2[DisclosureStates.Closed] = State.Closed, _match2))
  }, render({
    props: passthroughProps,
    slot,
    defaultTag: DEFAULT_DISCLOSURE_TAG,
    name: "Disclosure"
  }))));
}
var DEFAULT_BUTTON_TAG = "button";
var Button = forwardRefWithAs(function Button2(props, ref) {
  var _useDisclosureContext = useDisclosureContext([Disclosure.name, Button2.name].join(".")), state2 = _useDisclosureContext[0], dispatch = _useDisclosureContext[1];
  var internalButtonRef = (0, import_react16.useRef)(null);
  var buttonRef = useSyncRefs(internalButtonRef, ref);
  var panelContext = useDisclosurePanelContext();
  var isWithinPanel = panelContext === null ? false : panelContext === state2.panelId;
  var handleKeyDown = (0, import_react16.useCallback)(function(event) {
    var _document$getElementB;
    if (isWithinPanel) {
      if (state2.disclosureState === DisclosureStates.Closed)
        return;
      switch (event.key) {
        case Keys.Space:
        case Keys.Enter:
          event.preventDefault();
          event.stopPropagation();
          dispatch({
            type: ActionTypes2.ToggleDisclosure
          });
          (_document$getElementB = document.getElementById(state2.buttonId)) == null ? void 0 : _document$getElementB.focus();
          break;
      }
    } else {
      switch (event.key) {
        case Keys.Space:
        case Keys.Enter:
          event.preventDefault();
          event.stopPropagation();
          dispatch({
            type: ActionTypes2.ToggleDisclosure
          });
          break;
      }
    }
  }, [dispatch, isWithinPanel, state2.disclosureState]);
  var handleKeyUp = (0, import_react16.useCallback)(function(event) {
    switch (event.key) {
      case Keys.Space:
        event.preventDefault();
        break;
    }
  }, []);
  var handleClick = (0, import_react16.useCallback)(function(event) {
    if (isDisabledReactIssue7711(event.currentTarget))
      return;
    if (props.disabled)
      return;
    if (isWithinPanel) {
      var _document$getElementB2;
      dispatch({
        type: ActionTypes2.ToggleDisclosure
      });
      (_document$getElementB2 = document.getElementById(state2.buttonId)) == null ? void 0 : _document$getElementB2.focus();
    } else {
      dispatch({
        type: ActionTypes2.ToggleDisclosure
      });
    }
  }, [dispatch, props.disabled, state2.buttonId, isWithinPanel]);
  var slot = (0, import_react16.useMemo)(function() {
    return {
      open: state2.disclosureState === DisclosureStates.Open
    };
  }, [state2]);
  var type = useResolveButtonType(props, internalButtonRef);
  var passthroughProps = props;
  var propsWeControl = isWithinPanel ? {
    ref: buttonRef,
    type,
    onKeyDown: handleKeyDown,
    onClick: handleClick
  } : {
    ref: buttonRef,
    id: state2.buttonId,
    type,
    "aria-expanded": props.disabled ? void 0 : state2.disclosureState === DisclosureStates.Open,
    "aria-controls": state2.linkedPanel ? state2.panelId : void 0,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    onClick: handleClick
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_BUTTON_TAG,
    name: "Disclosure.Button"
  });
});
var DEFAULT_PANEL_TAG = "div";
var PanelRenderFeatures = Features.RenderStrategy | Features.Static;
var Panel = forwardRefWithAs(function Panel2(props, ref) {
  var _useDisclosureContext2 = useDisclosureContext([Disclosure.name, Panel2.name].join(".")), state2 = _useDisclosureContext2[0], dispatch = _useDisclosureContext2[1];
  var _useDisclosureAPICont = useDisclosureAPIContext([Disclosure.name, Panel2.name].join(".")), close = _useDisclosureAPICont.close;
  var panelRef = useSyncRefs(ref, function() {
    if (state2.linkedPanel)
      return;
    dispatch({
      type: ActionTypes2.LinkPanel
    });
  });
  var usesOpenClosedState = useOpenClosed();
  var visible = function() {
    if (usesOpenClosedState !== null) {
      return usesOpenClosedState === State.Open;
    }
    return state2.disclosureState === DisclosureStates.Open;
  }();
  (0, import_react16.useEffect)(function() {
    return function() {
      return dispatch({
        type: ActionTypes2.UnlinkPanel
      });
    };
  }, [dispatch]);
  (0, import_react16.useEffect)(function() {
    var _props$unmount;
    if (state2.disclosureState === DisclosureStates.Closed && ((_props$unmount = props.unmount) != null ? _props$unmount : true)) {
      dispatch({
        type: ActionTypes2.UnlinkPanel
      });
    }
  }, [state2.disclosureState, props.unmount, dispatch]);
  var slot = (0, import_react16.useMemo)(function() {
    return {
      open: state2.disclosureState === DisclosureStates.Open,
      close
    };
  }, [state2, close]);
  var propsWeControl = {
    ref: panelRef,
    id: state2.panelId
  };
  var passthroughProps = props;
  return import_react16.default.createElement(DisclosurePanelContext.Provider, {
    value: state2.panelId
  }, render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_PANEL_TAG,
    features: PanelRenderFeatures,
    visible,
    name: "Disclosure.Panel"
  }));
});
Disclosure.Button = Button;
Disclosure.Panel = Panel;

// node_modules/@headlessui/react/dist/components/focus-trap/focus-trap.esm.js
var import_react17 = __toESM(require_react());
var DEFAULT_FOCUS_TRAP_TAG = "div";
function FocusTrap(props) {
  var container = (0, import_react17.useRef)(null);
  var initialFocus = props.initialFocus, passthroughProps = _objectWithoutPropertiesLoose(props, ["initialFocus"]);
  var ready = useServerHandoffComplete();
  useFocusTrap(container, ready ? Features2.All : Features2.None, {
    initialFocus
  });
  var propsWeControl = {
    ref: container
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    defaultTag: DEFAULT_FOCUS_TRAP_TAG,
    name: "FocusTrap"
  });
}

// node_modules/@headlessui/react/dist/components/listbox/listbox.esm.js
var import_react20 = __toESM(require_react());

// node_modules/@headlessui/react/dist/utils/disposables.esm.js
function disposables() {
  var disposables2 = [];
  var api = {
    requestAnimationFrame: function(_requestAnimationFrame) {
      function requestAnimationFrame2() {
        return _requestAnimationFrame.apply(this, arguments);
      }
      requestAnimationFrame2.toString = function() {
        return _requestAnimationFrame.toString();
      };
      return requestAnimationFrame2;
    }(function() {
      var raf = requestAnimationFrame.apply(void 0, arguments);
      api.add(function() {
        return cancelAnimationFrame(raf);
      });
    }),
    nextFrame: function nextFrame() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      api.requestAnimationFrame(function() {
        api.requestAnimationFrame.apply(api, args);
      });
    },
    setTimeout: function(_setTimeout) {
      function setTimeout2() {
        return _setTimeout.apply(this, arguments);
      }
      setTimeout2.toString = function() {
        return _setTimeout.toString();
      };
      return setTimeout2;
    }(function() {
      var timer = setTimeout.apply(void 0, arguments);
      api.add(function() {
        return clearTimeout(timer);
      });
    }),
    add: function add(cb) {
      disposables2.push(cb);
    },
    dispose: function dispose() {
      for (var _iterator = _createForOfIteratorHelperLoose(disposables2.splice(0)), _step; !(_step = _iterator()).done; ) {
        var dispose2 = _step.value;
        dispose2();
      }
    }
  };
  return api;
}

// node_modules/@headlessui/react/dist/hooks/use-disposables.esm.js
var import_react18 = __toESM(require_react());
function useDisposables() {
  var _useState = (0, import_react18.useState)(disposables), d = _useState[0];
  (0, import_react18.useEffect)(function() {
    return function() {
      return d.dispose();
    };
  }, [d]);
  return d;
}

// node_modules/@headlessui/react/dist/hooks/use-computed.esm.js
var import_react19 = __toESM(require_react());
function useComputed(cb, dependencies) {
  var _useState = (0, import_react19.useState)(cb), value = _useState[0], setValue = _useState[1];
  var cbRef = (0, import_react19.useRef)(cb);
  useIsoMorphicEffect(function() {
    cbRef.current = cb;
  }, [cb]);
  useIsoMorphicEffect(function() {
    return setValue(cbRef.current);
  }, [cbRef, setValue].concat(dependencies));
  return value;
}

// node_modules/@headlessui/react/dist/utils/calculate-active-index.esm.js
function assertNever(x) {
  throw new Error("Unexpected object: " + x);
}
var Focus2;
(function(Focus3) {
  Focus3[Focus3["First"] = 0] = "First";
  Focus3[Focus3["Previous"] = 1] = "Previous";
  Focus3[Focus3["Next"] = 2] = "Next";
  Focus3[Focus3["Last"] = 3] = "Last";
  Focus3[Focus3["Specific"] = 4] = "Specific";
  Focus3[Focus3["Nothing"] = 5] = "Nothing";
})(Focus2 || (Focus2 = {}));
function calculateActiveIndex(action, resolvers) {
  var items = resolvers.resolveItems();
  if (items.length <= 0)
    return null;
  var currentActiveIndex = resolvers.resolveActiveIndex();
  var activeIndex = currentActiveIndex != null ? currentActiveIndex : -1;
  var nextActiveIndex = function() {
    switch (action.focus) {
      case Focus2.First:
        return items.findIndex(function(item) {
          return !resolvers.resolveDisabled(item);
        });
      case Focus2.Previous: {
        var idx = items.slice().reverse().findIndex(function(item, idx2, all) {
          if (activeIndex !== -1 && all.length - idx2 - 1 >= activeIndex)
            return false;
          return !resolvers.resolveDisabled(item);
        });
        if (idx === -1)
          return idx;
        return items.length - 1 - idx;
      }
      case Focus2.Next:
        return items.findIndex(function(item, idx2) {
          if (idx2 <= activeIndex)
            return false;
          return !resolvers.resolveDisabled(item);
        });
      case Focus2.Last: {
        var _idx = items.slice().reverse().findIndex(function(item) {
          return !resolvers.resolveDisabled(item);
        });
        if (_idx === -1)
          return _idx;
        return items.length - 1 - _idx;
      }
      case Focus2.Specific:
        return items.findIndex(function(item) {
          return resolvers.resolveId(item) === action.id;
        });
      case Focus2.Nothing:
        return null;
      default:
        assertNever(action);
    }
  }();
  return nextActiveIndex === -1 ? currentActiveIndex : nextActiveIndex;
}

// node_modules/@headlessui/react/dist/components/listbox/listbox.esm.js
var _reducers3;
var ListboxStates;
(function(ListboxStates2) {
  ListboxStates2[ListboxStates2["Open"] = 0] = "Open";
  ListboxStates2[ListboxStates2["Closed"] = 1] = "Closed";
})(ListboxStates || (ListboxStates = {}));
var ActionTypes3;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["OpenListbox"] = 0] = "OpenListbox";
  ActionTypes8[ActionTypes8["CloseListbox"] = 1] = "CloseListbox";
  ActionTypes8[ActionTypes8["SetDisabled"] = 2] = "SetDisabled";
  ActionTypes8[ActionTypes8["SetOrientation"] = 3] = "SetOrientation";
  ActionTypes8[ActionTypes8["GoToOption"] = 4] = "GoToOption";
  ActionTypes8[ActionTypes8["Search"] = 5] = "Search";
  ActionTypes8[ActionTypes8["ClearSearch"] = 6] = "ClearSearch";
  ActionTypes8[ActionTypes8["RegisterOption"] = 7] = "RegisterOption";
  ActionTypes8[ActionTypes8["UnregisterOption"] = 8] = "UnregisterOption";
})(ActionTypes3 || (ActionTypes3 = {}));
var reducers3 = (_reducers3 = {}, _reducers3[ActionTypes3.CloseListbox] = function(state2) {
  if (state2.disabled)
    return state2;
  if (state2.listboxState === ListboxStates.Closed)
    return state2;
  return _extends({}, state2, {
    activeOptionIndex: null,
    listboxState: ListboxStates.Closed
  });
}, _reducers3[ActionTypes3.OpenListbox] = function(state2) {
  if (state2.disabled)
    return state2;
  if (state2.listboxState === ListboxStates.Open)
    return state2;
  return _extends({}, state2, {
    listboxState: ListboxStates.Open
  });
}, _reducers3[ActionTypes3.SetDisabled] = function(state2, action) {
  if (state2.disabled === action.disabled)
    return state2;
  return _extends({}, state2, {
    disabled: action.disabled
  });
}, _reducers3[ActionTypes3.SetOrientation] = function(state2, action) {
  if (state2.orientation === action.orientation)
    return state2;
  return _extends({}, state2, {
    orientation: action.orientation
  });
}, _reducers3[ActionTypes3.GoToOption] = function(state2, action) {
  if (state2.disabled)
    return state2;
  if (state2.listboxState === ListboxStates.Closed)
    return state2;
  var activeOptionIndex = calculateActiveIndex(action, {
    resolveItems: function resolveItems() {
      return state2.options;
    },
    resolveActiveIndex: function resolveActiveIndex() {
      return state2.activeOptionIndex;
    },
    resolveId: function resolveId(item) {
      return item.id;
    },
    resolveDisabled: function resolveDisabled(item) {
      return item.dataRef.current.disabled;
    }
  });
  if (state2.searchQuery === "" && state2.activeOptionIndex === activeOptionIndex)
    return state2;
  return _extends({}, state2, {
    searchQuery: "",
    activeOptionIndex
  });
}, _reducers3[ActionTypes3.Search] = function(state2, action) {
  if (state2.disabled)
    return state2;
  if (state2.listboxState === ListboxStates.Closed)
    return state2;
  var searchQuery = state2.searchQuery + action.value.toLowerCase();
  var match2 = state2.options.findIndex(function(option) {
    var _option$dataRef$curre;
    return !option.dataRef.current.disabled && ((_option$dataRef$curre = option.dataRef.current.textValue) == null ? void 0 : _option$dataRef$curre.startsWith(searchQuery));
  });
  if (match2 === -1 || match2 === state2.activeOptionIndex)
    return _extends({}, state2, {
      searchQuery
    });
  return _extends({}, state2, {
    searchQuery,
    activeOptionIndex: match2
  });
}, _reducers3[ActionTypes3.ClearSearch] = function(state2) {
  if (state2.disabled)
    return state2;
  if (state2.listboxState === ListboxStates.Closed)
    return state2;
  if (state2.searchQuery === "")
    return state2;
  return _extends({}, state2, {
    searchQuery: ""
  });
}, _reducers3[ActionTypes3.RegisterOption] = function(state2, action) {
  return _extends({}, state2, {
    options: [].concat(state2.options, [{
      id: action.id,
      dataRef: action.dataRef
    }])
  });
}, _reducers3[ActionTypes3.UnregisterOption] = function(state2, action) {
  var nextOptions = state2.options.slice();
  var currentActiveOption = state2.activeOptionIndex !== null ? nextOptions[state2.activeOptionIndex] : null;
  var idx = nextOptions.findIndex(function(a) {
    return a.id === action.id;
  });
  if (idx !== -1)
    nextOptions.splice(idx, 1);
  return _extends({}, state2, {
    options: nextOptions,
    activeOptionIndex: function() {
      if (idx === state2.activeOptionIndex)
        return null;
      if (currentActiveOption === null)
        return null;
      return nextOptions.indexOf(currentActiveOption);
    }()
  });
}, _reducers3);
var ListboxContext = (0, import_react20.createContext)(null);
ListboxContext.displayName = "ListboxContext";
function useListboxContext(component) {
  var context = (0, import_react20.useContext)(ListboxContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Listbox.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useListboxContext);
    throw err;
  }
  return context;
}
function stateReducer3(state2, action) {
  return match(action.type, reducers3, state2, action);
}
var DEFAULT_LISTBOX_TAG = import_react20.Fragment;
function Listbox(props) {
  var _match;
  var value = props.value, onChange = props.onChange, _props$disabled = props.disabled, disabled = _props$disabled === void 0 ? false : _props$disabled, _props$horizontal = props.horizontal, horizontal = _props$horizontal === void 0 ? false : _props$horizontal, passThroughProps = _objectWithoutPropertiesLoose(props, ["value", "onChange", "disabled", "horizontal"]);
  var orientation = horizontal ? "horizontal" : "vertical";
  var reducerBag = (0, import_react20.useReducer)(stateReducer3, {
    listboxState: ListboxStates.Closed,
    propsRef: {
      current: {
        value,
        onChange
      }
    },
    labelRef: (0, import_react20.createRef)(),
    buttonRef: (0, import_react20.createRef)(),
    optionsRef: (0, import_react20.createRef)(),
    disabled,
    orientation,
    options: [],
    searchQuery: "",
    activeOptionIndex: null
  });
  var _reducerBag$ = reducerBag[0], listboxState = _reducerBag$.listboxState, propsRef = _reducerBag$.propsRef, optionsRef = _reducerBag$.optionsRef, buttonRef = _reducerBag$.buttonRef, dispatch = reducerBag[1];
  useIsoMorphicEffect(function() {
    propsRef.current.value = value;
  }, [value, propsRef]);
  useIsoMorphicEffect(function() {
    propsRef.current.onChange = onChange;
  }, [onChange, propsRef]);
  useIsoMorphicEffect(function() {
    return dispatch({
      type: ActionTypes3.SetDisabled,
      disabled
    });
  }, [disabled]);
  useIsoMorphicEffect(function() {
    return dispatch({
      type: ActionTypes3.SetOrientation,
      orientation
    });
  }, [orientation]);
  useWindowEvent("mousedown", function(event) {
    var _buttonRef$current, _optionsRef$current;
    var target = event.target;
    if (listboxState !== ListboxStates.Open)
      return;
    if ((_buttonRef$current = buttonRef.current) == null ? void 0 : _buttonRef$current.contains(target))
      return;
    if ((_optionsRef$current = optionsRef.current) == null ? void 0 : _optionsRef$current.contains(target))
      return;
    dispatch({
      type: ActionTypes3.CloseListbox
    });
    if (!isFocusableElement(target, FocusableMode.Loose)) {
      var _buttonRef$current2;
      event.preventDefault();
      (_buttonRef$current2 = buttonRef.current) == null ? void 0 : _buttonRef$current2.focus();
    }
  });
  var slot = (0, import_react20.useMemo)(function() {
    return {
      open: listboxState === ListboxStates.Open,
      disabled
    };
  }, [listboxState, disabled]);
  return import_react20.default.createElement(ListboxContext.Provider, {
    value: reducerBag
  }, import_react20.default.createElement(OpenClosedProvider, {
    value: match(listboxState, (_match = {}, _match[ListboxStates.Open] = State.Open, _match[ListboxStates.Closed] = State.Closed, _match))
  }, render({
    props: passThroughProps,
    slot,
    defaultTag: DEFAULT_LISTBOX_TAG,
    name: "Listbox"
  })));
}
var DEFAULT_BUTTON_TAG2 = "button";
var Button3 = forwardRefWithAs(function Button4(props, ref) {
  var _state$optionsRef$cur;
  var _useListboxContext = useListboxContext([Listbox.name, Button4.name].join(".")), state2 = _useListboxContext[0], dispatch = _useListboxContext[1];
  var buttonRef = useSyncRefs(state2.buttonRef, ref);
  var id2 = "headlessui-listbox-button-" + useId();
  var d = useDisposables();
  var handleKeyDown = (0, import_react20.useCallback)(function(event) {
    switch (event.key) {
      case Keys.Space:
      case Keys.Enter:
      case Keys.ArrowDown:
        event.preventDefault();
        dispatch({
          type: ActionTypes3.OpenListbox
        });
        d.nextFrame(function() {
          if (!state2.propsRef.current.value)
            dispatch({
              type: ActionTypes3.GoToOption,
              focus: Focus2.First
            });
        });
        break;
      case Keys.ArrowUp:
        event.preventDefault();
        dispatch({
          type: ActionTypes3.OpenListbox
        });
        d.nextFrame(function() {
          if (!state2.propsRef.current.value)
            dispatch({
              type: ActionTypes3.GoToOption,
              focus: Focus2.Last
            });
        });
        break;
    }
  }, [dispatch, state2, d]);
  var handleKeyUp = (0, import_react20.useCallback)(function(event) {
    switch (event.key) {
      case Keys.Space:
        event.preventDefault();
        break;
    }
  }, []);
  var handleClick = (0, import_react20.useCallback)(function(event) {
    if (isDisabledReactIssue7711(event.currentTarget))
      return event.preventDefault();
    if (state2.listboxState === ListboxStates.Open) {
      dispatch({
        type: ActionTypes3.CloseListbox
      });
      d.nextFrame(function() {
        var _state$buttonRef$curr;
        return (_state$buttonRef$curr = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr.focus({
          preventScroll: true
        });
      });
    } else {
      event.preventDefault();
      dispatch({
        type: ActionTypes3.OpenListbox
      });
    }
  }, [dispatch, d, state2]);
  var labelledby = useComputed(function() {
    if (!state2.labelRef.current)
      return void 0;
    return [state2.labelRef.current.id, id2].join(" ");
  }, [state2.labelRef.current, id2]);
  var slot = (0, import_react20.useMemo)(function() {
    return {
      open: state2.listboxState === ListboxStates.Open,
      disabled: state2.disabled
    };
  }, [state2]);
  var passthroughProps = props;
  var propsWeControl = {
    ref: buttonRef,
    id: id2,
    type: useResolveButtonType(props, state2.buttonRef),
    "aria-haspopup": true,
    "aria-controls": (_state$optionsRef$cur = state2.optionsRef.current) == null ? void 0 : _state$optionsRef$cur.id,
    "aria-expanded": state2.disabled ? void 0 : state2.listboxState === ListboxStates.Open,
    "aria-labelledby": labelledby,
    disabled: state2.disabled,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    onClick: handleClick
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_BUTTON_TAG2,
    name: "Listbox.Button"
  });
});
var DEFAULT_LABEL_TAG = "label";
function Label(props) {
  var _useListboxContext2 = useListboxContext([Listbox.name, Label.name].join(".")), state2 = _useListboxContext2[0];
  var id2 = "headlessui-listbox-label-" + useId();
  var handleClick = (0, import_react20.useCallback)(function() {
    var _state$buttonRef$curr2;
    return (_state$buttonRef$curr2 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr2.focus({
      preventScroll: true
    });
  }, [state2.buttonRef]);
  var slot = (0, import_react20.useMemo)(function() {
    return {
      open: state2.listboxState === ListboxStates.Open,
      disabled: state2.disabled
    };
  }, [state2]);
  var propsWeControl = {
    ref: state2.labelRef,
    id: id2,
    onClick: handleClick
  };
  return render({
    props: _extends({}, props, propsWeControl),
    slot,
    defaultTag: DEFAULT_LABEL_TAG,
    name: "Listbox.Label"
  });
}
var DEFAULT_OPTIONS_TAG = "ul";
var OptionsRenderFeatures = Features.RenderStrategy | Features.Static;
var Options = forwardRefWithAs(function Options2(props, ref) {
  var _state$options$state$;
  var _useListboxContext3 = useListboxContext([Listbox.name, Options2.name].join(".")), state2 = _useListboxContext3[0], dispatch = _useListboxContext3[1];
  var optionsRef = useSyncRefs(state2.optionsRef, ref);
  var id2 = "headlessui-listbox-options-" + useId();
  var d = useDisposables();
  var searchDisposables = useDisposables();
  var usesOpenClosedState = useOpenClosed();
  var visible = function() {
    if (usesOpenClosedState !== null) {
      return usesOpenClosedState === State.Open;
    }
    return state2.listboxState === ListboxStates.Open;
  }();
  useIsoMorphicEffect(function() {
    var container = state2.optionsRef.current;
    if (!container)
      return;
    if (state2.listboxState !== ListboxStates.Open)
      return;
    if (container === document.activeElement)
      return;
    container.focus({
      preventScroll: true
    });
  }, [state2.listboxState, state2.optionsRef]);
  var handleKeyDown = (0, import_react20.useCallback)(function(event) {
    searchDisposables.dispose();
    switch (event.key) {
      case Keys.Space:
        if (state2.searchQuery !== "") {
          event.preventDefault();
          event.stopPropagation();
          return dispatch({
            type: ActionTypes3.Search,
            value: event.key
          });
        }
      case Keys.Enter:
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes3.CloseListbox
        });
        if (state2.activeOptionIndex !== null) {
          var dataRef = state2.options[state2.activeOptionIndex].dataRef;
          state2.propsRef.current.onChange(dataRef.current.value);
        }
        disposables().nextFrame(function() {
          var _state$buttonRef$curr3;
          return (_state$buttonRef$curr3 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr3.focus({
            preventScroll: true
          });
        });
        break;
      case match(state2.orientation, {
        vertical: Keys.ArrowDown,
        horizontal: Keys.ArrowRight
      }):
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes3.GoToOption,
          focus: Focus2.Next
        });
      case match(state2.orientation, {
        vertical: Keys.ArrowUp,
        horizontal: Keys.ArrowLeft
      }):
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes3.GoToOption,
          focus: Focus2.Previous
        });
      case Keys.Home:
      case Keys.PageUp:
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes3.GoToOption,
          focus: Focus2.First
        });
      case Keys.End:
      case Keys.PageDown:
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes3.GoToOption,
          focus: Focus2.Last
        });
      case Keys.Escape:
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes3.CloseListbox
        });
        return d.nextFrame(function() {
          var _state$buttonRef$curr4;
          return (_state$buttonRef$curr4 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr4.focus({
            preventScroll: true
          });
        });
      case Keys.Tab:
        event.preventDefault();
        event.stopPropagation();
        break;
      default:
        if (event.key.length === 1) {
          dispatch({
            type: ActionTypes3.Search,
            value: event.key
          });
          searchDisposables.setTimeout(function() {
            return dispatch({
              type: ActionTypes3.ClearSearch
            });
          }, 350);
        }
        break;
    }
  }, [d, dispatch, searchDisposables, state2]);
  var labelledby = useComputed(function() {
    var _state$labelRef$curre, _state$labelRef$curre2, _state$buttonRef$curr5;
    return (_state$labelRef$curre = (_state$labelRef$curre2 = state2.labelRef.current) == null ? void 0 : _state$labelRef$curre2.id) != null ? _state$labelRef$curre : (_state$buttonRef$curr5 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr5.id;
  }, [state2.labelRef.current, state2.buttonRef.current]);
  var slot = (0, import_react20.useMemo)(function() {
    return {
      open: state2.listboxState === ListboxStates.Open
    };
  }, [state2]);
  var propsWeControl = {
    "aria-activedescendant": state2.activeOptionIndex === null ? void 0 : (_state$options$state$ = state2.options[state2.activeOptionIndex]) == null ? void 0 : _state$options$state$.id,
    "aria-labelledby": labelledby,
    "aria-orientation": state2.orientation,
    id: id2,
    onKeyDown: handleKeyDown,
    role: "listbox",
    tabIndex: 0,
    ref: optionsRef
  };
  var passthroughProps = props;
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_OPTIONS_TAG,
    features: OptionsRenderFeatures,
    visible,
    name: "Listbox.Options"
  });
});
var DEFAULT_OPTION_TAG = "li";
function Option(props) {
  var _props$disabled2 = props.disabled, disabled = _props$disabled2 === void 0 ? false : _props$disabled2, value = props.value, passthroughProps = _objectWithoutPropertiesLoose(props, ["disabled", "value"]);
  var _useListboxContext4 = useListboxContext([Listbox.name, Option.name].join(".")), state2 = _useListboxContext4[0], dispatch = _useListboxContext4[1];
  var id2 = "headlessui-listbox-option-" + useId();
  var active = state2.activeOptionIndex !== null ? state2.options[state2.activeOptionIndex].id === id2 : false;
  var selected = state2.propsRef.current.value === value;
  var bag = (0, import_react20.useRef)({
    disabled,
    value
  });
  useIsoMorphicEffect(function() {
    bag.current.disabled = disabled;
  }, [bag, disabled]);
  useIsoMorphicEffect(function() {
    bag.current.value = value;
  }, [bag, value]);
  useIsoMorphicEffect(function() {
    var _document$getElementB, _document$getElementB2;
    bag.current.textValue = (_document$getElementB = document.getElementById(id2)) == null ? void 0 : (_document$getElementB2 = _document$getElementB.textContent) == null ? void 0 : _document$getElementB2.toLowerCase();
  }, [bag, id2]);
  var select = (0, import_react20.useCallback)(function() {
    return state2.propsRef.current.onChange(value);
  }, [state2.propsRef, value]);
  useIsoMorphicEffect(function() {
    dispatch({
      type: ActionTypes3.RegisterOption,
      id: id2,
      dataRef: bag
    });
    return function() {
      return dispatch({
        type: ActionTypes3.UnregisterOption,
        id: id2
      });
    };
  }, [bag, id2]);
  useIsoMorphicEffect(function() {
    var _document$getElementB3;
    if (state2.listboxState !== ListboxStates.Open)
      return;
    if (!selected)
      return;
    dispatch({
      type: ActionTypes3.GoToOption,
      focus: Focus2.Specific,
      id: id2
    });
    (_document$getElementB3 = document.getElementById(id2)) == null ? void 0 : _document$getElementB3.focus == null ? void 0 : _document$getElementB3.focus();
  }, [state2.listboxState]);
  useIsoMorphicEffect(function() {
    if (state2.listboxState !== ListboxStates.Open)
      return;
    if (!active)
      return;
    var d = disposables();
    d.nextFrame(function() {
      var _document$getElementB4;
      return (_document$getElementB4 = document.getElementById(id2)) == null ? void 0 : _document$getElementB4.scrollIntoView == null ? void 0 : _document$getElementB4.scrollIntoView({
        block: "nearest"
      });
    });
    return d.dispose;
  }, [id2, active, state2.listboxState]);
  var handleClick = (0, import_react20.useCallback)(function(event) {
    if (disabled)
      return event.preventDefault();
    select();
    dispatch({
      type: ActionTypes3.CloseListbox
    });
    disposables().nextFrame(function() {
      var _state$buttonRef$curr6;
      return (_state$buttonRef$curr6 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr6.focus({
        preventScroll: true
      });
    });
  }, [dispatch, state2.buttonRef, disabled, select]);
  var handleFocus = (0, import_react20.useCallback)(function() {
    if (disabled)
      return dispatch({
        type: ActionTypes3.GoToOption,
        focus: Focus2.Nothing
      });
    dispatch({
      type: ActionTypes3.GoToOption,
      focus: Focus2.Specific,
      id: id2
    });
  }, [disabled, id2, dispatch]);
  var handleMove = (0, import_react20.useCallback)(function() {
    if (disabled)
      return;
    if (active)
      return;
    dispatch({
      type: ActionTypes3.GoToOption,
      focus: Focus2.Specific,
      id: id2
    });
  }, [disabled, active, id2, dispatch]);
  var handleLeave = (0, import_react20.useCallback)(function() {
    if (disabled)
      return;
    if (!active)
      return;
    dispatch({
      type: ActionTypes3.GoToOption,
      focus: Focus2.Nothing
    });
  }, [disabled, active, dispatch]);
  var slot = (0, import_react20.useMemo)(function() {
    return {
      active,
      selected,
      disabled
    };
  }, [active, selected, disabled]);
  var propsWeControl = {
    id: id2,
    role: "option",
    tabIndex: disabled === true ? void 0 : -1,
    "aria-disabled": disabled === true ? true : void 0,
    "aria-selected": selected === true ? true : void 0,
    disabled: void 0,
    onClick: handleClick,
    onFocus: handleFocus,
    onPointerMove: handleMove,
    onMouseMove: handleMove,
    onPointerLeave: handleLeave,
    onMouseLeave: handleLeave
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_OPTION_TAG,
    name: "Listbox.Option"
  });
}
Listbox.Button = Button3;
Listbox.Label = Label;
Listbox.Options = Options;
Listbox.Option = Option;

// node_modules/@headlessui/react/dist/components/menu/menu.esm.js
var import_react22 = __toESM(require_react());

// node_modules/@headlessui/react/dist/hooks/use-tree-walker.esm.js
var import_react21 = __toESM(require_react());
function useTreeWalker(_ref) {
  var container = _ref.container, accept = _ref.accept, walk = _ref.walk, _ref$enabled = _ref.enabled, enabled = _ref$enabled === void 0 ? true : _ref$enabled;
  var acceptRef = (0, import_react21.useRef)(accept);
  var walkRef = (0, import_react21.useRef)(walk);
  (0, import_react21.useEffect)(function() {
    acceptRef.current = accept;
    walkRef.current = walk;
  }, [accept, walk]);
  useIsoMorphicEffect(function() {
    if (!container)
      return;
    if (!enabled)
      return;
    var accept2 = acceptRef.current;
    var walk2 = walkRef.current;
    var acceptNode = Object.assign(function(node) {
      return accept2(node);
    }, {
      acceptNode: accept2
    });
    var walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, acceptNode, false);
    while (walker.nextNode()) {
      walk2(walker.currentNode);
    }
  }, [container, enabled, acceptRef, walkRef]);
}

// node_modules/@headlessui/react/dist/components/menu/menu.esm.js
var _reducers4;
var MenuStates;
(function(MenuStates2) {
  MenuStates2[MenuStates2["Open"] = 0] = "Open";
  MenuStates2[MenuStates2["Closed"] = 1] = "Closed";
})(MenuStates || (MenuStates = {}));
var ActionTypes4;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["OpenMenu"] = 0] = "OpenMenu";
  ActionTypes8[ActionTypes8["CloseMenu"] = 1] = "CloseMenu";
  ActionTypes8[ActionTypes8["GoToItem"] = 2] = "GoToItem";
  ActionTypes8[ActionTypes8["Search"] = 3] = "Search";
  ActionTypes8[ActionTypes8["ClearSearch"] = 4] = "ClearSearch";
  ActionTypes8[ActionTypes8["RegisterItem"] = 5] = "RegisterItem";
  ActionTypes8[ActionTypes8["UnregisterItem"] = 6] = "UnregisterItem";
})(ActionTypes4 || (ActionTypes4 = {}));
var reducers4 = (_reducers4 = {}, _reducers4[ActionTypes4.CloseMenu] = function(state2) {
  if (state2.menuState === MenuStates.Closed)
    return state2;
  return _extends({}, state2, {
    activeItemIndex: null,
    menuState: MenuStates.Closed
  });
}, _reducers4[ActionTypes4.OpenMenu] = function(state2) {
  if (state2.menuState === MenuStates.Open)
    return state2;
  return _extends({}, state2, {
    menuState: MenuStates.Open
  });
}, _reducers4[ActionTypes4.GoToItem] = function(state2, action) {
  var activeItemIndex = calculateActiveIndex(action, {
    resolveItems: function resolveItems() {
      return state2.items;
    },
    resolveActiveIndex: function resolveActiveIndex() {
      return state2.activeItemIndex;
    },
    resolveId: function resolveId(item) {
      return item.id;
    },
    resolveDisabled: function resolveDisabled(item) {
      return item.dataRef.current.disabled;
    }
  });
  if (state2.searchQuery === "" && state2.activeItemIndex === activeItemIndex)
    return state2;
  return _extends({}, state2, {
    searchQuery: "",
    activeItemIndex
  });
}, _reducers4[ActionTypes4.Search] = function(state2, action) {
  var searchQuery = state2.searchQuery + action.value.toLowerCase();
  var match2 = state2.items.findIndex(function(item) {
    var _item$dataRef$current;
    return ((_item$dataRef$current = item.dataRef.current.textValue) == null ? void 0 : _item$dataRef$current.startsWith(searchQuery)) && !item.dataRef.current.disabled;
  });
  if (match2 === -1 || match2 === state2.activeItemIndex)
    return _extends({}, state2, {
      searchQuery
    });
  return _extends({}, state2, {
    searchQuery,
    activeItemIndex: match2
  });
}, _reducers4[ActionTypes4.ClearSearch] = function(state2) {
  if (state2.searchQuery === "")
    return state2;
  return _extends({}, state2, {
    searchQuery: ""
  });
}, _reducers4[ActionTypes4.RegisterItem] = function(state2, action) {
  return _extends({}, state2, {
    items: [].concat(state2.items, [{
      id: action.id,
      dataRef: action.dataRef
    }])
  });
}, _reducers4[ActionTypes4.UnregisterItem] = function(state2, action) {
  var nextItems = state2.items.slice();
  var currentActiveItem = state2.activeItemIndex !== null ? nextItems[state2.activeItemIndex] : null;
  var idx = nextItems.findIndex(function(a) {
    return a.id === action.id;
  });
  if (idx !== -1)
    nextItems.splice(idx, 1);
  return _extends({}, state2, {
    items: nextItems,
    activeItemIndex: function() {
      if (idx === state2.activeItemIndex)
        return null;
      if (currentActiveItem === null)
        return null;
      return nextItems.indexOf(currentActiveItem);
    }()
  });
}, _reducers4);
var MenuContext = (0, import_react22.createContext)(null);
MenuContext.displayName = "MenuContext";
function useMenuContext(component) {
  var context = (0, import_react22.useContext)(MenuContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Menu.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useMenuContext);
    throw err;
  }
  return context;
}
function stateReducer4(state2, action) {
  return match(action.type, reducers4, state2, action);
}
var DEFAULT_MENU_TAG = import_react22.Fragment;
function Menu(props) {
  var _match;
  var reducerBag = (0, import_react22.useReducer)(stateReducer4, {
    menuState: MenuStates.Closed,
    buttonRef: (0, import_react22.createRef)(),
    itemsRef: (0, import_react22.createRef)(),
    items: [],
    searchQuery: "",
    activeItemIndex: null
  });
  var _reducerBag$ = reducerBag[0], menuState = _reducerBag$.menuState, itemsRef = _reducerBag$.itemsRef, buttonRef = _reducerBag$.buttonRef, dispatch = reducerBag[1];
  useWindowEvent("mousedown", function(event) {
    var _buttonRef$current, _itemsRef$current;
    var target = event.target;
    if (menuState !== MenuStates.Open)
      return;
    if ((_buttonRef$current = buttonRef.current) == null ? void 0 : _buttonRef$current.contains(target))
      return;
    if ((_itemsRef$current = itemsRef.current) == null ? void 0 : _itemsRef$current.contains(target))
      return;
    dispatch({
      type: ActionTypes4.CloseMenu
    });
    if (!isFocusableElement(target, FocusableMode.Loose)) {
      var _buttonRef$current2;
      event.preventDefault();
      (_buttonRef$current2 = buttonRef.current) == null ? void 0 : _buttonRef$current2.focus();
    }
  });
  var slot = (0, import_react22.useMemo)(function() {
    return {
      open: menuState === MenuStates.Open
    };
  }, [menuState]);
  return import_react22.default.createElement(MenuContext.Provider, {
    value: reducerBag
  }, import_react22.default.createElement(OpenClosedProvider, {
    value: match(menuState, (_match = {}, _match[MenuStates.Open] = State.Open, _match[MenuStates.Closed] = State.Closed, _match))
  }, render({
    props,
    slot,
    defaultTag: DEFAULT_MENU_TAG,
    name: "Menu"
  })));
}
var DEFAULT_BUTTON_TAG3 = "button";
var Button5 = forwardRefWithAs(function Button6(props, ref) {
  var _state$itemsRef$curre;
  var _useMenuContext = useMenuContext([Menu.name, Button6.name].join(".")), state2 = _useMenuContext[0], dispatch = _useMenuContext[1];
  var buttonRef = useSyncRefs(state2.buttonRef, ref);
  var id2 = "headlessui-menu-button-" + useId();
  var d = useDisposables();
  var handleKeyDown = (0, import_react22.useCallback)(function(event) {
    switch (event.key) {
      case Keys.Space:
      case Keys.Enter:
      case Keys.ArrowDown:
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes4.OpenMenu
        });
        d.nextFrame(function() {
          return dispatch({
            type: ActionTypes4.GoToItem,
            focus: Focus2.First
          });
        });
        break;
      case Keys.ArrowUp:
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes4.OpenMenu
        });
        d.nextFrame(function() {
          return dispatch({
            type: ActionTypes4.GoToItem,
            focus: Focus2.Last
          });
        });
        break;
    }
  }, [dispatch, d]);
  var handleKeyUp = (0, import_react22.useCallback)(function(event) {
    switch (event.key) {
      case Keys.Space:
        event.preventDefault();
        break;
    }
  }, []);
  var handleClick = (0, import_react22.useCallback)(function(event) {
    if (isDisabledReactIssue7711(event.currentTarget))
      return event.preventDefault();
    if (props.disabled)
      return;
    if (state2.menuState === MenuStates.Open) {
      dispatch({
        type: ActionTypes4.CloseMenu
      });
      d.nextFrame(function() {
        var _state$buttonRef$curr;
        return (_state$buttonRef$curr = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr.focus({
          preventScroll: true
        });
      });
    } else {
      event.preventDefault();
      event.stopPropagation();
      dispatch({
        type: ActionTypes4.OpenMenu
      });
    }
  }, [dispatch, d, state2, props.disabled]);
  var slot = (0, import_react22.useMemo)(function() {
    return {
      open: state2.menuState === MenuStates.Open
    };
  }, [state2]);
  var passthroughProps = props;
  var propsWeControl = {
    ref: buttonRef,
    id: id2,
    type: useResolveButtonType(props, state2.buttonRef),
    "aria-haspopup": true,
    "aria-controls": (_state$itemsRef$curre = state2.itemsRef.current) == null ? void 0 : _state$itemsRef$curre.id,
    "aria-expanded": props.disabled ? void 0 : state2.menuState === MenuStates.Open,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    onClick: handleClick
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_BUTTON_TAG3,
    name: "Menu.Button"
  });
});
var DEFAULT_ITEMS_TAG = "div";
var ItemsRenderFeatures = Features.RenderStrategy | Features.Static;
var Items = forwardRefWithAs(function Items2(props, ref) {
  var _state$items$state$ac, _state$buttonRef$curr4;
  var _useMenuContext2 = useMenuContext([Menu.name, Items2.name].join(".")), state2 = _useMenuContext2[0], dispatch = _useMenuContext2[1];
  var itemsRef = useSyncRefs(state2.itemsRef, ref);
  var id2 = "headlessui-menu-items-" + useId();
  var searchDisposables = useDisposables();
  var usesOpenClosedState = useOpenClosed();
  var visible = function() {
    if (usesOpenClosedState !== null) {
      return usesOpenClosedState === State.Open;
    }
    return state2.menuState === MenuStates.Open;
  }();
  (0, import_react22.useEffect)(function() {
    var container = state2.itemsRef.current;
    if (!container)
      return;
    if (state2.menuState !== MenuStates.Open)
      return;
    if (container === document.activeElement)
      return;
    container.focus({
      preventScroll: true
    });
  }, [state2.menuState, state2.itemsRef]);
  useTreeWalker({
    container: state2.itemsRef.current,
    enabled: state2.menuState === MenuStates.Open,
    accept: function accept(node) {
      if (node.getAttribute("role") === "menuitem")
        return NodeFilter.FILTER_REJECT;
      if (node.hasAttribute("role"))
        return NodeFilter.FILTER_SKIP;
      return NodeFilter.FILTER_ACCEPT;
    },
    walk: function walk(node) {
      node.setAttribute("role", "none");
    }
  });
  var handleKeyDown = (0, import_react22.useCallback)(function(event) {
    searchDisposables.dispose();
    switch (event.key) {
      case Keys.Space:
        if (state2.searchQuery !== "") {
          event.preventDefault();
          event.stopPropagation();
          return dispatch({
            type: ActionTypes4.Search,
            value: event.key
          });
        }
      case Keys.Enter:
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes4.CloseMenu
        });
        if (state2.activeItemIndex !== null) {
          var _document$getElementB;
          var _id = state2.items[state2.activeItemIndex].id;
          (_document$getElementB = document.getElementById(_id)) == null ? void 0 : _document$getElementB.click();
        }
        disposables().nextFrame(function() {
          var _state$buttonRef$curr2;
          return (_state$buttonRef$curr2 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr2.focus({
            preventScroll: true
          });
        });
        break;
      case Keys.ArrowDown:
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes4.GoToItem,
          focus: Focus2.Next
        });
      case Keys.ArrowUp:
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes4.GoToItem,
          focus: Focus2.Previous
        });
      case Keys.Home:
      case Keys.PageUp:
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes4.GoToItem,
          focus: Focus2.First
        });
      case Keys.End:
      case Keys.PageDown:
        event.preventDefault();
        event.stopPropagation();
        return dispatch({
          type: ActionTypes4.GoToItem,
          focus: Focus2.Last
        });
      case Keys.Escape:
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes4.CloseMenu
        });
        disposables().nextFrame(function() {
          var _state$buttonRef$curr3;
          return (_state$buttonRef$curr3 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr3.focus({
            preventScroll: true
          });
        });
        break;
      case Keys.Tab:
        event.preventDefault();
        event.stopPropagation();
        break;
      default:
        if (event.key.length === 1) {
          dispatch({
            type: ActionTypes4.Search,
            value: event.key
          });
          searchDisposables.setTimeout(function() {
            return dispatch({
              type: ActionTypes4.ClearSearch
            });
          }, 350);
        }
        break;
    }
  }, [dispatch, searchDisposables, state2]);
  var handleKeyUp = (0, import_react22.useCallback)(function(event) {
    switch (event.key) {
      case Keys.Space:
        event.preventDefault();
        break;
    }
  }, []);
  var slot = (0, import_react22.useMemo)(function() {
    return {
      open: state2.menuState === MenuStates.Open
    };
  }, [state2]);
  var propsWeControl = {
    "aria-activedescendant": state2.activeItemIndex === null ? void 0 : (_state$items$state$ac = state2.items[state2.activeItemIndex]) == null ? void 0 : _state$items$state$ac.id,
    "aria-labelledby": (_state$buttonRef$curr4 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr4.id,
    id: id2,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    role: "menu",
    tabIndex: 0,
    ref: itemsRef
  };
  var passthroughProps = props;
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_ITEMS_TAG,
    features: ItemsRenderFeatures,
    visible,
    name: "Menu.Items"
  });
});
var DEFAULT_ITEM_TAG = import_react22.Fragment;
function Item(props) {
  var _props$disabled = props.disabled, disabled = _props$disabled === void 0 ? false : _props$disabled, onClick = props.onClick, passthroughProps = _objectWithoutPropertiesLoose(props, ["disabled", "onClick"]);
  var _useMenuContext3 = useMenuContext([Menu.name, Item.name].join(".")), state2 = _useMenuContext3[0], dispatch = _useMenuContext3[1];
  var id2 = "headlessui-menu-item-" + useId();
  var active = state2.activeItemIndex !== null ? state2.items[state2.activeItemIndex].id === id2 : false;
  useIsoMorphicEffect(function() {
    if (state2.menuState !== MenuStates.Open)
      return;
    if (!active)
      return;
    var d = disposables();
    d.nextFrame(function() {
      var _document$getElementB2;
      return (_document$getElementB2 = document.getElementById(id2)) == null ? void 0 : _document$getElementB2.scrollIntoView == null ? void 0 : _document$getElementB2.scrollIntoView({
        block: "nearest"
      });
    });
    return d.dispose;
  }, [id2, active, state2.menuState]);
  var bag = (0, import_react22.useRef)({
    disabled
  });
  useIsoMorphicEffect(function() {
    bag.current.disabled = disabled;
  }, [bag, disabled]);
  useIsoMorphicEffect(function() {
    var _document$getElementB3, _document$getElementB4;
    bag.current.textValue = (_document$getElementB3 = document.getElementById(id2)) == null ? void 0 : (_document$getElementB4 = _document$getElementB3.textContent) == null ? void 0 : _document$getElementB4.toLowerCase();
  }, [bag, id2]);
  useIsoMorphicEffect(function() {
    dispatch({
      type: ActionTypes4.RegisterItem,
      id: id2,
      dataRef: bag
    });
    return function() {
      return dispatch({
        type: ActionTypes4.UnregisterItem,
        id: id2
      });
    };
  }, [bag, id2]);
  var handleClick = (0, import_react22.useCallback)(function(event) {
    if (disabled)
      return event.preventDefault();
    dispatch({
      type: ActionTypes4.CloseMenu
    });
    disposables().nextFrame(function() {
      var _state$buttonRef$curr5;
      return (_state$buttonRef$curr5 = state2.buttonRef.current) == null ? void 0 : _state$buttonRef$curr5.focus({
        preventScroll: true
      });
    });
    if (onClick)
      return onClick(event);
  }, [dispatch, state2.buttonRef, disabled, onClick]);
  var handleFocus = (0, import_react22.useCallback)(function() {
    if (disabled)
      return dispatch({
        type: ActionTypes4.GoToItem,
        focus: Focus2.Nothing
      });
    dispatch({
      type: ActionTypes4.GoToItem,
      focus: Focus2.Specific,
      id: id2
    });
  }, [disabled, id2, dispatch]);
  var handleMove = (0, import_react22.useCallback)(function() {
    if (disabled)
      return;
    if (active)
      return;
    dispatch({
      type: ActionTypes4.GoToItem,
      focus: Focus2.Specific,
      id: id2
    });
  }, [disabled, active, id2, dispatch]);
  var handleLeave = (0, import_react22.useCallback)(function() {
    if (disabled)
      return;
    if (!active)
      return;
    dispatch({
      type: ActionTypes4.GoToItem,
      focus: Focus2.Nothing
    });
  }, [disabled, active, dispatch]);
  var slot = (0, import_react22.useMemo)(function() {
    return {
      active,
      disabled
    };
  }, [active, disabled]);
  var propsWeControl = {
    id: id2,
    role: "menuitem",
    tabIndex: disabled === true ? void 0 : -1,
    "aria-disabled": disabled === true ? true : void 0,
    disabled: void 0,
    onClick: handleClick,
    onFocus: handleFocus,
    onPointerMove: handleMove,
    onMouseMove: handleMove,
    onPointerLeave: handleLeave,
    onMouseLeave: handleLeave
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_ITEM_TAG,
    name: "Menu.Item"
  });
}
Menu.Button = Button5;
Menu.Items = Items;
Menu.Item = Item;

// node_modules/@headlessui/react/dist/components/popover/popover.esm.js
var import_react23 = __toESM(require_react());
var _reducers5;
var PopoverStates;
(function(PopoverStates2) {
  PopoverStates2[PopoverStates2["Open"] = 0] = "Open";
  PopoverStates2[PopoverStates2["Closed"] = 1] = "Closed";
})(PopoverStates || (PopoverStates = {}));
var ActionTypes5;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["TogglePopover"] = 0] = "TogglePopover";
  ActionTypes8[ActionTypes8["ClosePopover"] = 1] = "ClosePopover";
  ActionTypes8[ActionTypes8["SetButton"] = 2] = "SetButton";
  ActionTypes8[ActionTypes8["SetButtonId"] = 3] = "SetButtonId";
  ActionTypes8[ActionTypes8["SetPanel"] = 4] = "SetPanel";
  ActionTypes8[ActionTypes8["SetPanelId"] = 5] = "SetPanelId";
})(ActionTypes5 || (ActionTypes5 = {}));
var reducers5 = (_reducers5 = {}, _reducers5[ActionTypes5.TogglePopover] = function(state2) {
  var _match;
  return _extends({}, state2, {
    popoverState: match(state2.popoverState, (_match = {}, _match[PopoverStates.Open] = PopoverStates.Closed, _match[PopoverStates.Closed] = PopoverStates.Open, _match))
  });
}, _reducers5[ActionTypes5.ClosePopover] = function(state2) {
  if (state2.popoverState === PopoverStates.Closed)
    return state2;
  return _extends({}, state2, {
    popoverState: PopoverStates.Closed
  });
}, _reducers5[ActionTypes5.SetButton] = function(state2, action) {
  if (state2.button === action.button)
    return state2;
  return _extends({}, state2, {
    button: action.button
  });
}, _reducers5[ActionTypes5.SetButtonId] = function(state2, action) {
  if (state2.buttonId === action.buttonId)
    return state2;
  return _extends({}, state2, {
    buttonId: action.buttonId
  });
}, _reducers5[ActionTypes5.SetPanel] = function(state2, action) {
  if (state2.panel === action.panel)
    return state2;
  return _extends({}, state2, {
    panel: action.panel
  });
}, _reducers5[ActionTypes5.SetPanelId] = function(state2, action) {
  if (state2.panelId === action.panelId)
    return state2;
  return _extends({}, state2, {
    panelId: action.panelId
  });
}, _reducers5);
var PopoverContext = (0, import_react23.createContext)(null);
PopoverContext.displayName = "PopoverContext";
function usePopoverContext(component) {
  var context = (0, import_react23.useContext)(PopoverContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Popover.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, usePopoverContext);
    throw err;
  }
  return context;
}
var PopoverAPIContext = (0, import_react23.createContext)(null);
PopoverAPIContext.displayName = "PopoverAPIContext";
function usePopoverAPIContext(component) {
  var context = (0, import_react23.useContext)(PopoverAPIContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + Popover.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, usePopoverAPIContext);
    throw err;
  }
  return context;
}
var PopoverGroupContext = (0, import_react23.createContext)(null);
PopoverGroupContext.displayName = "PopoverGroupContext";
function usePopoverGroupContext() {
  return (0, import_react23.useContext)(PopoverGroupContext);
}
var PopoverPanelContext = (0, import_react23.createContext)(null);
PopoverPanelContext.displayName = "PopoverPanelContext";
function usePopoverPanelContext() {
  return (0, import_react23.useContext)(PopoverPanelContext);
}
function stateReducer5(state2, action) {
  return match(action.type, reducers5, state2, action);
}
var DEFAULT_POPOVER_TAG = "div";
function Popover(props) {
  var _match2;
  var buttonId = "headlessui-popover-button-" + useId();
  var panelId = "headlessui-popover-panel-" + useId();
  var reducerBag = (0, import_react23.useReducer)(stateReducer5, {
    popoverState: PopoverStates.Closed,
    button: null,
    buttonId,
    panel: null,
    panelId
  });
  var _reducerBag$ = reducerBag[0], popoverState = _reducerBag$.popoverState, button = _reducerBag$.button, panel = _reducerBag$.panel, dispatch = reducerBag[1];
  (0, import_react23.useEffect)(function() {
    return dispatch({
      type: ActionTypes5.SetButtonId,
      buttonId
    });
  }, [buttonId, dispatch]);
  (0, import_react23.useEffect)(function() {
    return dispatch({
      type: ActionTypes5.SetPanelId,
      panelId
    });
  }, [panelId, dispatch]);
  var registerBag = (0, import_react23.useMemo)(function() {
    return {
      buttonId,
      panelId,
      close: function close2() {
        return dispatch({
          type: ActionTypes5.ClosePopover
        });
      }
    };
  }, [buttonId, panelId, dispatch]);
  var groupContext = usePopoverGroupContext();
  var registerPopover = groupContext == null ? void 0 : groupContext.registerPopover;
  var isFocusWithinPopoverGroup = (0, import_react23.useCallback)(function() {
    var _groupContext$isFocus;
    return (_groupContext$isFocus = groupContext == null ? void 0 : groupContext.isFocusWithinPopoverGroup()) != null ? _groupContext$isFocus : (button == null ? void 0 : button.contains(document.activeElement)) || (panel == null ? void 0 : panel.contains(document.activeElement));
  }, [groupContext, button, panel]);
  (0, import_react23.useEffect)(function() {
    return registerPopover == null ? void 0 : registerPopover(registerBag);
  }, [registerPopover, registerBag]);
  useWindowEvent("focus", function() {
    if (popoverState !== PopoverStates.Open)
      return;
    if (isFocusWithinPopoverGroup())
      return;
    if (!button)
      return;
    if (!panel)
      return;
    dispatch({
      type: ActionTypes5.ClosePopover
    });
  }, true);
  useWindowEvent("mousedown", function(event) {
    var target = event.target;
    if (popoverState !== PopoverStates.Open)
      return;
    if (button == null ? void 0 : button.contains(target))
      return;
    if (panel == null ? void 0 : panel.contains(target))
      return;
    dispatch({
      type: ActionTypes5.ClosePopover
    });
    if (!isFocusableElement(target, FocusableMode.Loose)) {
      event.preventDefault();
      button == null ? void 0 : button.focus();
    }
  });
  var close = (0, import_react23.useCallback)(function(focusableElement) {
    dispatch({
      type: ActionTypes5.ClosePopover
    });
    var restoreElement = function() {
      if (!focusableElement)
        return button;
      if (focusableElement instanceof HTMLElement)
        return focusableElement;
      if (focusableElement.current instanceof HTMLElement)
        return focusableElement.current;
      return button;
    }();
    restoreElement == null ? void 0 : restoreElement.focus();
  }, [dispatch, button]);
  var api = (0, import_react23.useMemo)(function() {
    return {
      close
    };
  }, [close]);
  var slot = (0, import_react23.useMemo)(function() {
    return {
      open: popoverState === PopoverStates.Open,
      close
    };
  }, [popoverState, close]);
  return import_react23.default.createElement(PopoverContext.Provider, {
    value: reducerBag
  }, import_react23.default.createElement(PopoverAPIContext.Provider, {
    value: api
  }, import_react23.default.createElement(OpenClosedProvider, {
    value: match(popoverState, (_match2 = {}, _match2[PopoverStates.Open] = State.Open, _match2[PopoverStates.Closed] = State.Closed, _match2))
  }, render({
    props,
    slot,
    defaultTag: DEFAULT_POPOVER_TAG,
    name: "Popover"
  }))));
}
var DEFAULT_BUTTON_TAG4 = "button";
var Button7 = forwardRefWithAs(function Button8(props, ref) {
  var _usePopoverContext = usePopoverContext([Popover.name, Button8.name].join(".")), state2 = _usePopoverContext[0], dispatch = _usePopoverContext[1];
  var internalButtonRef = (0, import_react23.useRef)(null);
  var groupContext = usePopoverGroupContext();
  var closeOthers = groupContext == null ? void 0 : groupContext.closeOthers;
  var panelContext = usePopoverPanelContext();
  var isWithinPanel = panelContext === null ? false : panelContext === state2.panelId;
  var buttonRef = useSyncRefs(internalButtonRef, ref, isWithinPanel ? null : function(button) {
    return dispatch({
      type: ActionTypes5.SetButton,
      button
    });
  });
  var withinPanelButtonRef = useSyncRefs(internalButtonRef, ref);
  var activeElementRef = (0, import_react23.useRef)(null);
  var previousActiveElementRef = (0, import_react23.useRef)(typeof window === "undefined" ? null : document.activeElement);
  useWindowEvent("focus", function() {
    previousActiveElementRef.current = activeElementRef.current;
    activeElementRef.current = document.activeElement;
  }, true);
  var handleKeyDown = (0, import_react23.useCallback)(function(event) {
    var _state$button;
    if (isWithinPanel) {
      if (state2.popoverState === PopoverStates.Closed)
        return;
      switch (event.key) {
        case Keys.Space:
        case Keys.Enter:
          event.preventDefault();
          event.stopPropagation();
          dispatch({
            type: ActionTypes5.ClosePopover
          });
          (_state$button = state2.button) == null ? void 0 : _state$button.focus();
          break;
      }
    } else {
      switch (event.key) {
        case Keys.Space:
        case Keys.Enter:
          event.preventDefault();
          event.stopPropagation();
          if (state2.popoverState === PopoverStates.Closed)
            closeOthers == null ? void 0 : closeOthers(state2.buttonId);
          dispatch({
            type: ActionTypes5.TogglePopover
          });
          break;
        case Keys.Escape:
          if (state2.popoverState !== PopoverStates.Open)
            return closeOthers == null ? void 0 : closeOthers(state2.buttonId);
          if (!internalButtonRef.current)
            return;
          if (!internalButtonRef.current.contains(document.activeElement))
            return;
          event.preventDefault();
          event.stopPropagation();
          dispatch({
            type: ActionTypes5.ClosePopover
          });
          break;
        case Keys.Tab:
          if (state2.popoverState !== PopoverStates.Open)
            return;
          if (!state2.panel)
            return;
          if (!state2.button)
            return;
          if (event.shiftKey) {
            var _state$button2;
            if (!previousActiveElementRef.current)
              return;
            if ((_state$button2 = state2.button) == null ? void 0 : _state$button2.contains(previousActiveElementRef.current))
              return;
            if (state2.panel.contains(previousActiveElementRef.current))
              return;
            var focusableElements = getFocusableElements();
            var previousIdx = focusableElements.indexOf(previousActiveElementRef.current);
            var buttonIdx = focusableElements.indexOf(state2.button);
            if (buttonIdx > previousIdx)
              return;
            event.preventDefault();
            event.stopPropagation();
            focusIn(state2.panel, Focus.Last);
          } else {
            event.preventDefault();
            event.stopPropagation();
            focusIn(state2.panel, Focus.First);
          }
          break;
      }
    }
  }, [dispatch, state2.popoverState, state2.buttonId, state2.button, state2.panel, internalButtonRef, closeOthers, isWithinPanel]);
  var handleKeyUp = (0, import_react23.useCallback)(function(event) {
    var _state$button3;
    if (isWithinPanel)
      return;
    if (event.key === Keys.Space) {
      event.preventDefault();
    }
    if (state2.popoverState !== PopoverStates.Open)
      return;
    if (!state2.panel)
      return;
    if (!state2.button)
      return;
    switch (event.key) {
      case Keys.Tab:
        if (!previousActiveElementRef.current)
          return;
        if ((_state$button3 = state2.button) == null ? void 0 : _state$button3.contains(previousActiveElementRef.current))
          return;
        if (state2.panel.contains(previousActiveElementRef.current))
          return;
        var focusableElements = getFocusableElements();
        var previousIdx = focusableElements.indexOf(previousActiveElementRef.current);
        var buttonIdx = focusableElements.indexOf(state2.button);
        if (buttonIdx > previousIdx)
          return;
        event.preventDefault();
        event.stopPropagation();
        focusIn(state2.panel, Focus.Last);
        break;
    }
  }, [state2.popoverState, state2.panel, state2.button, isWithinPanel]);
  var handleClick = (0, import_react23.useCallback)(function(event) {
    if (isDisabledReactIssue7711(event.currentTarget))
      return;
    if (props.disabled)
      return;
    if (isWithinPanel) {
      var _state$button4;
      dispatch({
        type: ActionTypes5.ClosePopover
      });
      (_state$button4 = state2.button) == null ? void 0 : _state$button4.focus();
    } else {
      var _state$button5;
      if (state2.popoverState === PopoverStates.Closed)
        closeOthers == null ? void 0 : closeOthers(state2.buttonId);
      (_state$button5 = state2.button) == null ? void 0 : _state$button5.focus();
      dispatch({
        type: ActionTypes5.TogglePopover
      });
    }
  }, [dispatch, state2.button, state2.popoverState, state2.buttonId, props.disabled, closeOthers, isWithinPanel]);
  var slot = (0, import_react23.useMemo)(function() {
    return {
      open: state2.popoverState === PopoverStates.Open
    };
  }, [state2]);
  var type = useResolveButtonType(props, internalButtonRef);
  var passthroughProps = props;
  var propsWeControl = isWithinPanel ? {
    ref: withinPanelButtonRef,
    type,
    onKeyDown: handleKeyDown,
    onClick: handleClick
  } : {
    ref: buttonRef,
    id: state2.buttonId,
    type,
    "aria-expanded": props.disabled ? void 0 : state2.popoverState === PopoverStates.Open,
    "aria-controls": state2.panel ? state2.panelId : void 0,
    onKeyDown: handleKeyDown,
    onKeyUp: handleKeyUp,
    onClick: handleClick
  };
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_BUTTON_TAG4,
    name: "Popover.Button"
  });
});
var DEFAULT_OVERLAY_TAG2 = "div";
var OverlayRenderFeatures = Features.RenderStrategy | Features.Static;
var Overlay3 = forwardRefWithAs(function Overlay4(props, ref) {
  var _usePopoverContext2 = usePopoverContext([Popover.name, Overlay4.name].join(".")), popoverState = _usePopoverContext2[0].popoverState, dispatch = _usePopoverContext2[1];
  var overlayRef = useSyncRefs(ref);
  var id2 = "headlessui-popover-overlay-" + useId();
  var usesOpenClosedState = useOpenClosed();
  var visible = function() {
    if (usesOpenClosedState !== null) {
      return usesOpenClosedState === State.Open;
    }
    return popoverState === PopoverStates.Open;
  }();
  var handleClick = (0, import_react23.useCallback)(function(event) {
    if (isDisabledReactIssue7711(event.currentTarget))
      return event.preventDefault();
    dispatch({
      type: ActionTypes5.ClosePopover
    });
  }, [dispatch]);
  var slot = (0, import_react23.useMemo)(function() {
    return {
      open: popoverState === PopoverStates.Open
    };
  }, [popoverState]);
  var propsWeControl = {
    ref: overlayRef,
    id: id2,
    "aria-hidden": true,
    onClick: handleClick
  };
  var passthroughProps = props;
  return render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_OVERLAY_TAG2,
    features: OverlayRenderFeatures,
    visible,
    name: "Popover.Overlay"
  });
});
var DEFAULT_PANEL_TAG2 = "div";
var PanelRenderFeatures2 = Features.RenderStrategy | Features.Static;
var Panel3 = forwardRefWithAs(function Panel4(props, ref) {
  var _props$focus = props.focus, focus = _props$focus === void 0 ? false : _props$focus, passthroughProps = _objectWithoutPropertiesLoose(props, ["focus"]);
  var _usePopoverContext3 = usePopoverContext([Popover.name, Panel4.name].join(".")), state2 = _usePopoverContext3[0], dispatch = _usePopoverContext3[1];
  var _usePopoverAPIContext = usePopoverAPIContext([Popover.name, Panel4.name].join(".")), close = _usePopoverAPIContext.close;
  var internalPanelRef = (0, import_react23.useRef)(null);
  var panelRef = useSyncRefs(internalPanelRef, ref, function(panel) {
    dispatch({
      type: ActionTypes5.SetPanel,
      panel
    });
  });
  var usesOpenClosedState = useOpenClosed();
  var visible = function() {
    if (usesOpenClosedState !== null) {
      return usesOpenClosedState === State.Open;
    }
    return state2.popoverState === PopoverStates.Open;
  }();
  var handleKeyDown = (0, import_react23.useCallback)(function(event) {
    var _state$button6;
    switch (event.key) {
      case Keys.Escape:
        if (state2.popoverState !== PopoverStates.Open)
          return;
        if (!internalPanelRef.current)
          return;
        if (!internalPanelRef.current.contains(document.activeElement))
          return;
        event.preventDefault();
        event.stopPropagation();
        dispatch({
          type: ActionTypes5.ClosePopover
        });
        (_state$button6 = state2.button) == null ? void 0 : _state$button6.focus();
        break;
    }
  }, [state2, internalPanelRef, dispatch]);
  (0, import_react23.useEffect)(function() {
    return function() {
      return dispatch({
        type: ActionTypes5.SetPanel,
        panel: null
      });
    };
  }, [dispatch]);
  (0, import_react23.useEffect)(function() {
    var _props$unmount;
    if (props["static"])
      return;
    if (state2.popoverState === PopoverStates.Closed && ((_props$unmount = props.unmount) != null ? _props$unmount : true)) {
      dispatch({
        type: ActionTypes5.SetPanel,
        panel: null
      });
    }
  }, [state2.popoverState, props.unmount, props["static"], dispatch]);
  (0, import_react23.useEffect)(function() {
    if (!focus)
      return;
    if (state2.popoverState !== PopoverStates.Open)
      return;
    if (!internalPanelRef.current)
      return;
    var activeElement = document.activeElement;
    if (internalPanelRef.current.contains(activeElement))
      return;
    focusIn(internalPanelRef.current, Focus.First);
  }, [focus, internalPanelRef, state2.popoverState]);
  useWindowEvent("keydown", function(event) {
    if (state2.popoverState !== PopoverStates.Open)
      return;
    if (!internalPanelRef.current)
      return;
    if (event.key !== Keys.Tab)
      return;
    if (!document.activeElement)
      return;
    if (!internalPanelRef.current)
      return;
    if (!internalPanelRef.current.contains(document.activeElement))
      return;
    event.preventDefault();
    var result = focusIn(internalPanelRef.current, event.shiftKey ? Focus.Previous : Focus.Next);
    if (result === FocusResult.Underflow) {
      var _state$button7;
      return (_state$button7 = state2.button) == null ? void 0 : _state$button7.focus();
    } else if (result === FocusResult.Overflow) {
      if (!state2.button)
        return;
      var elements = getFocusableElements();
      var buttonIdx = elements.indexOf(state2.button);
      var nextElements = elements.splice(buttonIdx + 1).filter(function(element) {
        var _internalPanelRef$cur;
        return !((_internalPanelRef$cur = internalPanelRef.current) == null ? void 0 : _internalPanelRef$cur.contains(element));
      });
      if (focusIn(nextElements, Focus.First) === FocusResult.Error) {
        focusIn(document.body, Focus.First);
      }
    }
  });
  useWindowEvent("focus", function() {
    var _internalPanelRef$cur2;
    if (!focus)
      return;
    if (state2.popoverState !== PopoverStates.Open)
      return;
    if (!internalPanelRef.current)
      return;
    if ((_internalPanelRef$cur2 = internalPanelRef.current) == null ? void 0 : _internalPanelRef$cur2.contains(document.activeElement))
      return;
    dispatch({
      type: ActionTypes5.ClosePopover
    });
  }, true);
  var slot = (0, import_react23.useMemo)(function() {
    return {
      open: state2.popoverState === PopoverStates.Open,
      close
    };
  }, [state2, close]);
  var propsWeControl = {
    ref: panelRef,
    id: state2.panelId,
    onKeyDown: handleKeyDown
  };
  return import_react23.default.createElement(PopoverPanelContext.Provider, {
    value: state2.panelId
  }, render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_PANEL_TAG2,
    features: PanelRenderFeatures2,
    visible,
    name: "Popover.Panel"
  }));
});
var DEFAULT_GROUP_TAG2 = "div";
function Group2(props) {
  var groupRef = (0, import_react23.useRef)(null);
  var _useState = (0, import_react23.useState)([]), popovers = _useState[0], setPopovers = _useState[1];
  var unregisterPopover = (0, import_react23.useCallback)(function(registerbag) {
    setPopovers(function(existing) {
      var idx = existing.indexOf(registerbag);
      if (idx !== -1) {
        var clone = existing.slice();
        clone.splice(idx, 1);
        return clone;
      }
      return existing;
    });
  }, [setPopovers]);
  var registerPopover = (0, import_react23.useCallback)(function(registerbag) {
    setPopovers(function(existing) {
      return [].concat(existing, [registerbag]);
    });
    return function() {
      return unregisterPopover(registerbag);
    };
  }, [setPopovers, unregisterPopover]);
  var isFocusWithinPopoverGroup = (0, import_react23.useCallback)(function() {
    var _groupRef$current;
    var element = document.activeElement;
    if ((_groupRef$current = groupRef.current) == null ? void 0 : _groupRef$current.contains(element))
      return true;
    return popovers.some(function(bag) {
      var _document$getElementB, _document$getElementB2;
      return ((_document$getElementB = document.getElementById(bag.buttonId)) == null ? void 0 : _document$getElementB.contains(element)) || ((_document$getElementB2 = document.getElementById(bag.panelId)) == null ? void 0 : _document$getElementB2.contains(element));
    });
  }, [groupRef, popovers]);
  var closeOthers = (0, import_react23.useCallback)(function(buttonId) {
    for (var _iterator = _createForOfIteratorHelperLoose(popovers), _step; !(_step = _iterator()).done; ) {
      var popover = _step.value;
      if (popover.buttonId !== buttonId)
        popover.close();
    }
  }, [popovers]);
  var contextBag = (0, import_react23.useMemo)(function() {
    return {
      registerPopover,
      unregisterPopover,
      isFocusWithinPopoverGroup,
      closeOthers
    };
  }, [registerPopover, unregisterPopover, isFocusWithinPopoverGroup, closeOthers]);
  var slot = (0, import_react23.useMemo)(function() {
    return {};
  }, []);
  var propsWeControl = {
    ref: groupRef
  };
  var passthroughProps = props;
  return import_react23.default.createElement(PopoverGroupContext.Provider, {
    value: contextBag
  }, render({
    props: _extends({}, passthroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_GROUP_TAG2,
    name: "Popover.Group"
  }));
}
Popover.Button = Button7;
Popover.Overlay = Overlay3;
Popover.Panel = Panel3;
Popover.Group = Group2;

// node_modules/@headlessui/react/dist/components/radio-group/radio-group.esm.js
var import_react26 = __toESM(require_react());

// node_modules/@headlessui/react/dist/hooks/use-flags.esm.js
var import_react24 = __toESM(require_react());
function useFlags(initialFlags) {
  if (initialFlags === void 0) {
    initialFlags = 0;
  }
  var _useState = (0, import_react24.useState)(initialFlags), flags = _useState[0], setFlags = _useState[1];
  var addFlag = (0, import_react24.useCallback)(function(flag) {
    return setFlags(function(flags2) {
      return flags2 | flag;
    });
  }, [setFlags]);
  var hasFlag = (0, import_react24.useCallback)(function(flag) {
    return Boolean(flags & flag);
  }, [flags]);
  var removeFlag = (0, import_react24.useCallback)(function(flag) {
    return setFlags(function(flags2) {
      return flags2 & ~flag;
    });
  }, [setFlags]);
  var toggleFlag = (0, import_react24.useCallback)(function(flag) {
    return setFlags(function(flags2) {
      return flags2 ^ flag;
    });
  }, [setFlags]);
  return {
    addFlag,
    hasFlag,
    removeFlag,
    toggleFlag
  };
}

// node_modules/@headlessui/react/dist/components/label/label.esm.js
var import_react25 = __toESM(require_react());
var LabelContext = (0, import_react25.createContext)(null);
function useLabelContext() {
  var context = (0, import_react25.useContext)(LabelContext);
  if (context === null) {
    var err = new Error("You used a <Label /> component, but it is not inside a relevant parent.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useLabelContext);
    throw err;
  }
  return context;
}
function useLabels() {
  var _useState = (0, import_react25.useState)([]), labelIds = _useState[0], setLabelIds = _useState[1];
  return [
    labelIds.length > 0 ? labelIds.join(" ") : void 0,
    (0, import_react25.useMemo)(function() {
      return function LabelProvider(props) {
        var register = (0, import_react25.useCallback)(function(value) {
          setLabelIds(function(existing) {
            return [].concat(existing, [value]);
          });
          return function() {
            return setLabelIds(function(existing) {
              var clone = existing.slice();
              var idx = clone.indexOf(value);
              if (idx !== -1)
                clone.splice(idx, 1);
              return clone;
            });
          };
        }, []);
        var contextBag = (0, import_react25.useMemo)(function() {
          return {
            register,
            slot: props.slot,
            name: props.name,
            props: props.props
          };
        }, [register, props.slot, props.name, props.props]);
        return import_react25.default.createElement(LabelContext.Provider, {
          value: contextBag
        }, props.children);
      };
    }, [setLabelIds])
  ];
}
var DEFAULT_LABEL_TAG2 = "label";
function Label2(props) {
  var _props$passive = props.passive, passive = _props$passive === void 0 ? false : _props$passive, passThroughProps = _objectWithoutPropertiesLoose(props, ["passive"]);
  var context = useLabelContext();
  var id2 = "headlessui-label-" + useId();
  useIsoMorphicEffect(function() {
    return context.register(id2);
  }, [id2, context.register]);
  var propsWeControl = _extends({}, context.props, {
    id: id2
  });
  var allProps = _extends({}, passThroughProps, propsWeControl);
  if (passive)
    delete allProps["onClick"];
  return render({
    props: allProps,
    slot: context.slot || {},
    defaultTag: DEFAULT_LABEL_TAG2,
    name: context.name || "Label"
  });
}

// node_modules/@headlessui/react/dist/components/radio-group/radio-group.esm.js
var _reducers6;
var ActionTypes6;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["RegisterOption"] = 0] = "RegisterOption";
  ActionTypes8[ActionTypes8["UnregisterOption"] = 1] = "UnregisterOption";
})(ActionTypes6 || (ActionTypes6 = {}));
var reducers6 = (_reducers6 = {}, _reducers6[ActionTypes6.RegisterOption] = function(state2, action) {
  return _extends({}, state2, {
    options: [].concat(state2.options, [{
      id: action.id,
      element: action.element,
      propsRef: action.propsRef
    }])
  });
}, _reducers6[ActionTypes6.UnregisterOption] = function(state2, action) {
  var options = state2.options.slice();
  var idx = state2.options.findIndex(function(radio) {
    return radio.id === action.id;
  });
  if (idx === -1)
    return state2;
  options.splice(idx, 1);
  return _extends({}, state2, {
    options
  });
}, _reducers6);
var RadioGroupContext = (0, import_react26.createContext)(null);
RadioGroupContext.displayName = "RadioGroupContext";
function useRadioGroupContext(component) {
  var context = (0, import_react26.useContext)(RadioGroupContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <" + RadioGroup.name + " /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useRadioGroupContext);
    throw err;
  }
  return context;
}
function stateReducer6(state2, action) {
  return match(action.type, reducers6, state2, action);
}
var DEFAULT_RADIO_GROUP_TAG = "div";
function RadioGroup(props) {
  var value = props.value, onChange = props.onChange, _props$disabled = props.disabled, disabled = _props$disabled === void 0 ? false : _props$disabled, passThroughProps = _objectWithoutPropertiesLoose(props, ["value", "onChange", "disabled"]);
  var _useReducer = (0, import_react26.useReducer)(stateReducer6, {
    options: []
  }), options = _useReducer[0].options, dispatch = _useReducer[1];
  var _useLabels = useLabels(), labelledby = _useLabels[0], LabelProvider = _useLabels[1];
  var _useDescriptions = useDescriptions(), describedby = _useDescriptions[0], DescriptionProvider = _useDescriptions[1];
  var id2 = "headlessui-radiogroup-" + useId();
  var radioGroupRef = (0, import_react26.useRef)(null);
  var firstOption = (0, import_react26.useMemo)(function() {
    return options.find(function(option) {
      if (option.propsRef.current.disabled)
        return false;
      return true;
    });
  }, [options]);
  var containsCheckedOption = (0, import_react26.useMemo)(function() {
    return options.some(function(option) {
      return option.propsRef.current.value === value;
    });
  }, [options, value]);
  var triggerChange = (0, import_react26.useCallback)(function(nextValue) {
    var _options$find;
    if (disabled)
      return false;
    if (nextValue === value)
      return false;
    var nextOption = (_options$find = options.find(function(option) {
      return option.propsRef.current.value === nextValue;
    })) == null ? void 0 : _options$find.propsRef.current;
    if (nextOption == null ? void 0 : nextOption.disabled)
      return false;
    onChange(nextValue);
    return true;
  }, [onChange, value, disabled, options]);
  useTreeWalker({
    container: radioGroupRef.current,
    accept: function accept(node) {
      if (node.getAttribute("role") === "radio")
        return NodeFilter.FILTER_REJECT;
      if (node.hasAttribute("role"))
        return NodeFilter.FILTER_SKIP;
      return NodeFilter.FILTER_ACCEPT;
    },
    walk: function walk(node) {
      node.setAttribute("role", "none");
    }
  });
  var handleKeyDown = (0, import_react26.useCallback)(function(event) {
    var container = radioGroupRef.current;
    if (!container)
      return;
    var all = options.filter(function(option) {
      return option.propsRef.current.disabled === false;
    }).map(function(radio) {
      return radio.element.current;
    });
    switch (event.key) {
      case Keys.ArrowLeft:
      case Keys.ArrowUp:
        {
          event.preventDefault();
          event.stopPropagation();
          var result = focusIn(all, Focus.Previous | Focus.WrapAround);
          if (result === FocusResult.Success) {
            var activeOption = options.find(function(option) {
              return option.element.current === document.activeElement;
            });
            if (activeOption)
              triggerChange(activeOption.propsRef.current.value);
          }
        }
        break;
      case Keys.ArrowRight:
      case Keys.ArrowDown:
        {
          event.preventDefault();
          event.stopPropagation();
          var _result = focusIn(all, Focus.Next | Focus.WrapAround);
          if (_result === FocusResult.Success) {
            var _activeOption = options.find(function(option) {
              return option.element.current === document.activeElement;
            });
            if (_activeOption)
              triggerChange(_activeOption.propsRef.current.value);
          }
        }
        break;
      case Keys.Space:
        {
          event.preventDefault();
          event.stopPropagation();
          var _activeOption2 = options.find(function(option) {
            return option.element.current === document.activeElement;
          });
          if (_activeOption2)
            triggerChange(_activeOption2.propsRef.current.value);
        }
        break;
    }
  }, [radioGroupRef, options, triggerChange]);
  var registerOption = (0, import_react26.useCallback)(function(option) {
    dispatch(_extends({
      type: ActionTypes6.RegisterOption
    }, option));
    return function() {
      return dispatch({
        type: ActionTypes6.UnregisterOption,
        id: option.id
      });
    };
  }, [dispatch]);
  var api = (0, import_react26.useMemo)(function() {
    return {
      registerOption,
      firstOption,
      containsCheckedOption,
      change: triggerChange,
      disabled,
      value
    };
  }, [registerOption, firstOption, containsCheckedOption, triggerChange, disabled, value]);
  var propsWeControl = {
    ref: radioGroupRef,
    id: id2,
    role: "radiogroup",
    "aria-labelledby": labelledby,
    "aria-describedby": describedby,
    onKeyDown: handleKeyDown
  };
  return import_react26.default.createElement(DescriptionProvider, {
    name: "RadioGroup.Description"
  }, import_react26.default.createElement(LabelProvider, {
    name: "RadioGroup.Label"
  }, import_react26.default.createElement(RadioGroupContext.Provider, {
    value: api
  }, render({
    props: _extends({}, passThroughProps, propsWeControl),
    defaultTag: DEFAULT_RADIO_GROUP_TAG,
    name: "RadioGroup"
  }))));
}
var OptionState;
(function(OptionState2) {
  OptionState2[OptionState2["Empty"] = 1] = "Empty";
  OptionState2[OptionState2["Active"] = 2] = "Active";
})(OptionState || (OptionState = {}));
var DEFAULT_OPTION_TAG2 = "div";
function Option2(props) {
  var optionRef = (0, import_react26.useRef)(null);
  var id2 = "headlessui-radiogroup-option-" + useId();
  var _useLabels2 = useLabels(), labelledby = _useLabels2[0], LabelProvider = _useLabels2[1];
  var _useDescriptions2 = useDescriptions(), describedby = _useDescriptions2[0], DescriptionProvider = _useDescriptions2[1];
  var _useFlags = useFlags(OptionState.Empty), addFlag = _useFlags.addFlag, removeFlag = _useFlags.removeFlag, hasFlag = _useFlags.hasFlag;
  var value = props.value, _props$disabled2 = props.disabled, disabled = _props$disabled2 === void 0 ? false : _props$disabled2, passThroughProps = _objectWithoutPropertiesLoose(props, ["value", "disabled"]);
  var propsRef = (0, import_react26.useRef)({
    value,
    disabled
  });
  useIsoMorphicEffect(function() {
    propsRef.current.value = value;
  }, [value, propsRef]);
  useIsoMorphicEffect(function() {
    propsRef.current.disabled = disabled;
  }, [disabled, propsRef]);
  var _useRadioGroupContext = useRadioGroupContext([RadioGroup.name, Option2.name].join(".")), registerOption = _useRadioGroupContext.registerOption, radioGroupDisabled = _useRadioGroupContext.disabled, change = _useRadioGroupContext.change, firstOption = _useRadioGroupContext.firstOption, containsCheckedOption = _useRadioGroupContext.containsCheckedOption, radioGroupValue = _useRadioGroupContext.value;
  useIsoMorphicEffect(function() {
    return registerOption({
      id: id2,
      element: optionRef,
      propsRef
    });
  }, [id2, registerOption, optionRef, props]);
  var handleClick = (0, import_react26.useCallback)(function() {
    var _optionRef$current;
    if (!change(value))
      return;
    addFlag(OptionState.Active);
    (_optionRef$current = optionRef.current) == null ? void 0 : _optionRef$current.focus();
  }, [addFlag, change, value]);
  var handleFocus = (0, import_react26.useCallback)(function() {
    return addFlag(OptionState.Active);
  }, [addFlag]);
  var handleBlur = (0, import_react26.useCallback)(function() {
    return removeFlag(OptionState.Active);
  }, [removeFlag]);
  var isFirstOption = (firstOption == null ? void 0 : firstOption.id) === id2;
  var isDisabled = radioGroupDisabled || disabled;
  var checked = radioGroupValue === value;
  var propsWeControl = {
    ref: optionRef,
    id: id2,
    role: "radio",
    "aria-checked": checked ? "true" : "false",
    "aria-labelledby": labelledby,
    "aria-describedby": describedby,
    "aria-disabled": isDisabled ? true : void 0,
    tabIndex: function() {
      if (isDisabled)
        return -1;
      if (checked)
        return 0;
      if (!containsCheckedOption && isFirstOption)
        return 0;
      return -1;
    }(),
    onClick: isDisabled ? void 0 : handleClick,
    onFocus: isDisabled ? void 0 : handleFocus,
    onBlur: isDisabled ? void 0 : handleBlur
  };
  var slot = (0, import_react26.useMemo)(function() {
    return {
      checked,
      disabled: isDisabled,
      active: hasFlag(OptionState.Active)
    };
  }, [checked, isDisabled, hasFlag]);
  return import_react26.default.createElement(DescriptionProvider, {
    name: "RadioGroup.Description"
  }, import_react26.default.createElement(LabelProvider, {
    name: "RadioGroup.Label"
  }, render({
    props: _extends({}, passThroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_OPTION_TAG2,
    name: "RadioGroup.Option"
  })));
}
RadioGroup.Option = Option2;
RadioGroup.Label = Label2;
RadioGroup.Description = Description;

// node_modules/@headlessui/react/dist/components/switch/switch.esm.js
var import_react27 = __toESM(require_react());
var GroupContext = (0, import_react27.createContext)(null);
GroupContext.displayName = "GroupContext";
var DEFAULT_GROUP_TAG3 = import_react27.Fragment;
function Group3(props) {
  var _useState = (0, import_react27.useState)(null), switchElement = _useState[0], setSwitchElement = _useState[1];
  var _useLabels = useLabels(), labelledby = _useLabels[0], LabelProvider = _useLabels[1];
  var _useDescriptions = useDescriptions(), describedby = _useDescriptions[0], DescriptionProvider = _useDescriptions[1];
  var context = (0, import_react27.useMemo)(function() {
    return {
      "switch": switchElement,
      setSwitch: setSwitchElement,
      labelledby,
      describedby
    };
  }, [switchElement, setSwitchElement, labelledby, describedby]);
  return import_react27.default.createElement(DescriptionProvider, {
    name: "Switch.Description"
  }, import_react27.default.createElement(LabelProvider, {
    name: "Switch.Label",
    props: {
      onClick: function onClick() {
        if (!switchElement)
          return;
        switchElement.click();
        switchElement.focus({
          preventScroll: true
        });
      }
    }
  }, import_react27.default.createElement(GroupContext.Provider, {
    value: context
  }, render({
    props,
    defaultTag: DEFAULT_GROUP_TAG3,
    name: "Switch.Group"
  }))));
}
var DEFAULT_SWITCH_TAG = "button";
function Switch(props) {
  var checked = props.checked, onChange = props.onChange, passThroughProps = _objectWithoutPropertiesLoose(props, ["checked", "onChange"]);
  var id2 = "headlessui-switch-" + useId();
  var groupContext = (0, import_react27.useContext)(GroupContext);
  var internalSwitchRef = (0, import_react27.useRef)(null);
  var switchRef = useSyncRefs(internalSwitchRef, groupContext === null ? null : groupContext.setSwitch);
  var toggle = (0, import_react27.useCallback)(function() {
    return onChange(!checked);
  }, [onChange, checked]);
  var handleClick = (0, import_react27.useCallback)(function(event) {
    if (isDisabledReactIssue7711(event.currentTarget))
      return event.preventDefault();
    event.preventDefault();
    toggle();
  }, [toggle]);
  var handleKeyUp = (0, import_react27.useCallback)(function(event) {
    if (event.key !== Keys.Tab)
      event.preventDefault();
    if (event.key === Keys.Space)
      toggle();
  }, [toggle]);
  var handleKeyPress = (0, import_react27.useCallback)(function(event) {
    return event.preventDefault();
  }, []);
  var slot = (0, import_react27.useMemo)(function() {
    return {
      checked
    };
  }, [checked]);
  var propsWeControl = {
    id: id2,
    ref: switchRef,
    role: "switch",
    type: useResolveButtonType(props, internalSwitchRef),
    tabIndex: 0,
    "aria-checked": checked,
    "aria-labelledby": groupContext == null ? void 0 : groupContext.labelledby,
    "aria-describedby": groupContext == null ? void 0 : groupContext.describedby,
    onClick: handleClick,
    onKeyUp: handleKeyUp,
    onKeyPress: handleKeyPress
  };
  return render({
    props: _extends({}, passThroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_SWITCH_TAG,
    name: "Switch"
  });
}
Switch.Group = Group3;
Switch.Label = Label2;
Switch.Description = Description;

// node_modules/@headlessui/react/dist/components/tabs/tabs.esm.js
var import_react28 = __toESM(require_react());
var _reducers7;
var ActionTypes7;
(function(ActionTypes8) {
  ActionTypes8[ActionTypes8["SetSelectedIndex"] = 0] = "SetSelectedIndex";
  ActionTypes8[ActionTypes8["SetOrientation"] = 1] = "SetOrientation";
  ActionTypes8[ActionTypes8["SetActivation"] = 2] = "SetActivation";
  ActionTypes8[ActionTypes8["RegisterTab"] = 3] = "RegisterTab";
  ActionTypes8[ActionTypes8["UnregisterTab"] = 4] = "UnregisterTab";
  ActionTypes8[ActionTypes8["RegisterPanel"] = 5] = "RegisterPanel";
  ActionTypes8[ActionTypes8["UnregisterPanel"] = 6] = "UnregisterPanel";
  ActionTypes8[ActionTypes8["ForceRerender"] = 7] = "ForceRerender";
})(ActionTypes7 || (ActionTypes7 = {}));
var reducers7 = (_reducers7 = {}, _reducers7[ActionTypes7.SetSelectedIndex] = function(state2, action) {
  if (state2.selectedIndex === action.index)
    return state2;
  return _extends({}, state2, {
    selectedIndex: action.index
  });
}, _reducers7[ActionTypes7.SetOrientation] = function(state2, action) {
  if (state2.orientation === action.orientation)
    return state2;
  return _extends({}, state2, {
    orientation: action.orientation
  });
}, _reducers7[ActionTypes7.SetActivation] = function(state2, action) {
  if (state2.activation === action.activation)
    return state2;
  return _extends({}, state2, {
    activation: action.activation
  });
}, _reducers7[ActionTypes7.RegisterTab] = function(state2, action) {
  if (state2.tabs.includes(action.tab))
    return state2;
  return _extends({}, state2, {
    tabs: [].concat(state2.tabs, [action.tab])
  });
}, _reducers7[ActionTypes7.UnregisterTab] = function(state2, action) {
  return _extends({}, state2, {
    tabs: state2.tabs.filter(function(tab) {
      return tab !== action.tab;
    })
  });
}, _reducers7[ActionTypes7.RegisterPanel] = function(state2, action) {
  if (state2.panels.includes(action.panel))
    return state2;
  return _extends({}, state2, {
    panels: [].concat(state2.panels, [action.panel])
  });
}, _reducers7[ActionTypes7.UnregisterPanel] = function(state2, action) {
  return _extends({}, state2, {
    panels: state2.panels.filter(function(panel) {
      return panel !== action.panel;
    })
  });
}, _reducers7[ActionTypes7.ForceRerender] = function(state2) {
  return _extends({}, state2);
}, _reducers7);
var TabsContext = (0, import_react28.createContext)(null);
TabsContext.displayName = "TabsContext";
function useTabsContext(component) {
  var context = (0, import_react28.useContext)(TabsContext);
  if (context === null) {
    var err = new Error("<" + component + " /> is missing a parent <Tab.Group /> component.");
    if (Error.captureStackTrace)
      Error.captureStackTrace(err, useTabsContext);
    throw err;
  }
  return context;
}
function stateReducer7(state2, action) {
  return match(action.type, reducers7, state2, action);
}
var DEFAULT_TABS_TAG = import_react28.Fragment;
function Tabs(props) {
  var _props$defaultIndex = props.defaultIndex, defaultIndex = _props$defaultIndex === void 0 ? 0 : _props$defaultIndex, _props$vertical = props.vertical, vertical = _props$vertical === void 0 ? false : _props$vertical, _props$manual = props.manual, manual = _props$manual === void 0 ? false : _props$manual, onChange = props.onChange, _props$selectedIndex = props.selectedIndex, selectedIndex = _props$selectedIndex === void 0 ? null : _props$selectedIndex, passThroughProps = _objectWithoutPropertiesLoose(props, ["defaultIndex", "vertical", "manual", "onChange", "selectedIndex"]);
  var orientation = vertical ? "vertical" : "horizontal";
  var activation = manual ? "manual" : "auto";
  var _useReducer = (0, import_react28.useReducer)(stateReducer7, {
    selectedIndex: null,
    tabs: [],
    panels: [],
    orientation,
    activation
  }), state2 = _useReducer[0], dispatch = _useReducer[1];
  var slot = (0, import_react28.useMemo)(function() {
    return {
      selectedIndex: state2.selectedIndex
    };
  }, [state2.selectedIndex]);
  var onChangeRef = (0, import_react28.useRef)(function() {
  });
  (0, import_react28.useEffect)(function() {
    dispatch({
      type: ActionTypes7.SetOrientation,
      orientation
    });
  }, [orientation]);
  (0, import_react28.useEffect)(function() {
    dispatch({
      type: ActionTypes7.SetActivation,
      activation
    });
  }, [activation]);
  (0, import_react28.useEffect)(function() {
    if (typeof onChange === "function") {
      onChangeRef.current = onChange;
    }
  }, [onChange]);
  (0, import_react28.useEffect)(function() {
    if (state2.tabs.length <= 0)
      return;
    if (selectedIndex === null && state2.selectedIndex !== null)
      return;
    var tabs = state2.tabs.map(function(tab) {
      return tab.current;
    }).filter(Boolean);
    var focusableTabs = tabs.filter(function(tab) {
      return !tab.hasAttribute("disabled");
    });
    var indexToSet = selectedIndex != null ? selectedIndex : defaultIndex;
    if (indexToSet < 0) {
      dispatch({
        type: ActionTypes7.SetSelectedIndex,
        index: tabs.indexOf(focusableTabs[0])
      });
    } else if (indexToSet > state2.tabs.length) {
      dispatch({
        type: ActionTypes7.SetSelectedIndex,
        index: tabs.indexOf(focusableTabs[focusableTabs.length - 1])
      });
    } else {
      var before = tabs.slice(0, indexToSet);
      var after = tabs.slice(indexToSet);
      var next = [].concat(after, before).find(function(tab) {
        return focusableTabs.includes(tab);
      });
      if (!next)
        return;
      dispatch({
        type: ActionTypes7.SetSelectedIndex,
        index: tabs.indexOf(next)
      });
    }
  }, [defaultIndex, selectedIndex, state2.tabs, state2.selectedIndex]);
  var lastChangedIndex = (0, import_react28.useRef)(state2.selectedIndex);
  var providerBag = (0, import_react28.useMemo)(function() {
    return [state2, {
      dispatch,
      change: function change(index) {
        if (lastChangedIndex.current !== index)
          onChangeRef.current(index);
        lastChangedIndex.current = index;
        dispatch({
          type: ActionTypes7.SetSelectedIndex,
          index
        });
      }
    }];
  }, [state2, dispatch]);
  return import_react28.default.createElement(TabsContext.Provider, {
    value: providerBag
  }, render({
    props: _extends({}, passThroughProps),
    slot,
    defaultTag: DEFAULT_TABS_TAG,
    name: "Tabs"
  }));
}
var DEFAULT_LIST_TAG = "div";
function List(props) {
  var _useTabsContext = useTabsContext([Tab.name, List.name].join(".")), _useTabsContext$ = _useTabsContext[0], selectedIndex = _useTabsContext$.selectedIndex, orientation = _useTabsContext$.orientation;
  var slot = {
    selectedIndex
  };
  var propsWeControl = {
    role: "tablist",
    "aria-orientation": orientation
  };
  var passThroughProps = props;
  return render({
    props: _extends({}, passThroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_LIST_TAG,
    name: "Tabs.List"
  });
}
var DEFAULT_TAB_TAG = "button";
function Tab(props) {
  var _panels$myIndex, _panels$myIndex$curre;
  var id2 = "headlessui-tabs-tab-" + useId();
  var _useTabsContext2 = useTabsContext(Tab.name), _useTabsContext2$ = _useTabsContext2[0], selectedIndex = _useTabsContext2$.selectedIndex, tabs = _useTabsContext2$.tabs, panels = _useTabsContext2$.panels, orientation = _useTabsContext2$.orientation, activation = _useTabsContext2$.activation, _useTabsContext2$2 = _useTabsContext2[1], dispatch = _useTabsContext2$2.dispatch, change = _useTabsContext2$2.change;
  var internalTabRef = (0, import_react28.useRef)(null);
  var tabRef = useSyncRefs(internalTabRef, function(element) {
    if (!element)
      return;
    dispatch({
      type: ActionTypes7.ForceRerender
    });
  });
  useIsoMorphicEffect(function() {
    dispatch({
      type: ActionTypes7.RegisterTab,
      tab: internalTabRef
    });
    return function() {
      return dispatch({
        type: ActionTypes7.UnregisterTab,
        tab: internalTabRef
      });
    };
  }, [dispatch, internalTabRef]);
  var myIndex = tabs.indexOf(internalTabRef);
  var selected = myIndex === selectedIndex;
  var handleKeyDown = (0, import_react28.useCallback)(function(event) {
    var list = tabs.map(function(tab) {
      return tab.current;
    }).filter(Boolean);
    if (event.key === Keys.Space || event.key === Keys.Enter) {
      event.preventDefault();
      event.stopPropagation();
      change(myIndex);
      return;
    }
    switch (event.key) {
      case Keys.Home:
      case Keys.PageUp:
        event.preventDefault();
        event.stopPropagation();
        return focusIn(list, Focus.First);
      case Keys.End:
      case Keys.PageDown:
        event.preventDefault();
        event.stopPropagation();
        return focusIn(list, Focus.Last);
    }
    return match(orientation, {
      vertical: function vertical() {
        if (event.key === Keys.ArrowUp)
          return focusIn(list, Focus.Previous | Focus.WrapAround);
        if (event.key === Keys.ArrowDown)
          return focusIn(list, Focus.Next | Focus.WrapAround);
        return;
      },
      horizontal: function horizontal() {
        if (event.key === Keys.ArrowLeft)
          return focusIn(list, Focus.Previous | Focus.WrapAround);
        if (event.key === Keys.ArrowRight)
          return focusIn(list, Focus.Next | Focus.WrapAround);
        return;
      }
    });
  }, [tabs, orientation, myIndex, change]);
  var handleFocus = (0, import_react28.useCallback)(function() {
    var _internalTabRef$curre;
    (_internalTabRef$curre = internalTabRef.current) == null ? void 0 : _internalTabRef$curre.focus();
  }, [internalTabRef]);
  var handleSelection = (0, import_react28.useCallback)(function() {
    var _internalTabRef$curre2;
    (_internalTabRef$curre2 = internalTabRef.current) == null ? void 0 : _internalTabRef$curre2.focus();
    change(myIndex);
  }, [change, myIndex, internalTabRef]);
  var slot = (0, import_react28.useMemo)(function() {
    return {
      selected
    };
  }, [selected]);
  var propsWeControl = {
    ref: tabRef,
    onKeyDown: handleKeyDown,
    onFocus: activation === "manual" ? handleFocus : handleSelection,
    onClick: handleSelection,
    id: id2,
    role: "tab",
    type: useResolveButtonType(props, internalTabRef),
    "aria-controls": (_panels$myIndex = panels[myIndex]) == null ? void 0 : (_panels$myIndex$curre = _panels$myIndex.current) == null ? void 0 : _panels$myIndex$curre.id,
    "aria-selected": selected,
    tabIndex: selected ? 0 : -1
  };
  var passThroughProps = props;
  return render({
    props: _extends({}, passThroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_TAB_TAG,
    name: "Tabs.Tab"
  });
}
var DEFAULT_PANELS_TAG = "div";
function Panels(props) {
  var _useTabsContext3 = useTabsContext([Tab.name, Panels.name].join(".")), selectedIndex = _useTabsContext3[0].selectedIndex;
  var slot = (0, import_react28.useMemo)(function() {
    return {
      selectedIndex
    };
  }, [selectedIndex]);
  return render({
    props,
    slot,
    defaultTag: DEFAULT_PANELS_TAG,
    name: "Tabs.Panels"
  });
}
var DEFAULT_PANEL_TAG3 = "div";
var PanelRenderFeatures3 = Features.RenderStrategy | Features.Static;
function Panel5(props) {
  var _tabs$myIndex, _tabs$myIndex$current;
  var _useTabsContext4 = useTabsContext([Tab.name, Panel5.name].join(".")), _useTabsContext4$ = _useTabsContext4[0], selectedIndex = _useTabsContext4$.selectedIndex, tabs = _useTabsContext4$.tabs, panels = _useTabsContext4$.panels, dispatch = _useTabsContext4[1].dispatch;
  var id2 = "headlessui-tabs-panel-" + useId();
  var internalPanelRef = (0, import_react28.useRef)(null);
  var panelRef = useSyncRefs(internalPanelRef, function(element) {
    if (!element)
      return;
    dispatch({
      type: ActionTypes7.ForceRerender
    });
  });
  useIsoMorphicEffect(function() {
    dispatch({
      type: ActionTypes7.RegisterPanel,
      panel: internalPanelRef
    });
    return function() {
      return dispatch({
        type: ActionTypes7.UnregisterPanel,
        panel: internalPanelRef
      });
    };
  }, [dispatch, internalPanelRef]);
  var myIndex = panels.indexOf(internalPanelRef);
  var selected = myIndex === selectedIndex;
  var slot = (0, import_react28.useMemo)(function() {
    return {
      selected
    };
  }, [selected]);
  var propsWeControl = {
    ref: panelRef,
    id: id2,
    role: "tabpanel",
    "aria-labelledby": (_tabs$myIndex = tabs[myIndex]) == null ? void 0 : (_tabs$myIndex$current = _tabs$myIndex.current) == null ? void 0 : _tabs$myIndex$current.id,
    tabIndex: selected ? 0 : -1
  };
  var passThroughProps = props;
  return render({
    props: _extends({}, passThroughProps, propsWeControl),
    slot,
    defaultTag: DEFAULT_PANEL_TAG3,
    features: PanelRenderFeatures3,
    visible: selected,
    name: "Tabs.Panel"
  });
}
Tab.Group = Tabs;
Tab.List = List;
Tab.Panels = Panels;
Tab.Panel = Panel5;

// node_modules/@headlessui/react/dist/components/transitions/transition.esm.js
var import_react30 = __toESM(require_react());

// node_modules/@headlessui/react/dist/hooks/use-is-initial-render.esm.js
var import_react29 = __toESM(require_react());
function useIsInitialRender() {
  var initial = (0, import_react29.useRef)(true);
  (0, import_react29.useEffect)(function() {
    initial.current = false;
  }, []);
  return initial.current;
}

// node_modules/@headlessui/react/dist/utils/once.esm.js
function once(cb) {
  var state2 = {
    called: false
  };
  return function() {
    if (state2.called)
      return;
    state2.called = true;
    return cb.apply(void 0, arguments);
  };
}

// node_modules/@headlessui/react/dist/components/transitions/utils/transition.esm.js
function addClasses(node) {
  var _node$classList;
  for (var _len = arguments.length, classes = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    classes[_key - 1] = arguments[_key];
  }
  node && classes.length > 0 && (_node$classList = node.classList).add.apply(_node$classList, classes);
}
function removeClasses(node) {
  var _node$classList2;
  for (var _len2 = arguments.length, classes = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
    classes[_key2 - 1] = arguments[_key2];
  }
  node && classes.length > 0 && (_node$classList2 = node.classList).remove.apply(_node$classList2, classes);
}
var Reason;
(function(Reason2) {
  Reason2["Finished"] = "finished";
  Reason2["Cancelled"] = "cancelled";
})(Reason || (Reason = {}));
function waitForTransition(node, done) {
  var d = disposables();
  if (!node)
    return d.dispose;
  var _getComputedStyle = getComputedStyle(node), transitionDuration = _getComputedStyle.transitionDuration, transitionDelay = _getComputedStyle.transitionDelay;
  var _map = [transitionDuration, transitionDelay].map(function(value) {
    var _value$split$filter$m = value.split(",").filter(Boolean).map(function(v) {
      return v.includes("ms") ? parseFloat(v) : parseFloat(v) * 1e3;
    }).sort(function(a, z) {
      return z - a;
    }), _value$split$filter$m2 = _value$split$filter$m[0], resolvedValue = _value$split$filter$m2 === void 0 ? 0 : _value$split$filter$m2;
    return resolvedValue;
  }), durationMs = _map[0], delaysMs = _map[1];
  if (durationMs !== 0) {
    d.setTimeout(function() {
      done(Reason.Finished);
    }, durationMs + delaysMs);
  } else {
    done(Reason.Finished);
  }
  d.add(function() {
    return done(Reason.Cancelled);
  });
  return d.dispose;
}
function transition(node, base, from, to, entered, done) {
  var d = disposables();
  var _done = done !== void 0 ? once(done) : function() {
  };
  removeClasses.apply(void 0, [node].concat(entered));
  addClasses.apply(void 0, [node].concat(base, from));
  d.nextFrame(function() {
    removeClasses.apply(void 0, [node].concat(from));
    addClasses.apply(void 0, [node].concat(to));
    d.add(waitForTransition(node, function(reason) {
      removeClasses.apply(void 0, [node].concat(to, base));
      addClasses.apply(void 0, [node].concat(entered));
      return _done(reason);
    }));
  });
  d.add(function() {
    return removeClasses.apply(void 0, [node].concat(base, from, to, entered));
  });
  d.add(function() {
    return _done(Reason.Cancelled);
  });
  return d.dispose;
}

// node_modules/@headlessui/react/dist/components/transitions/transition.esm.js
function useSplitClasses(classes) {
  if (classes === void 0) {
    classes = "";
  }
  return (0, import_react30.useMemo)(function() {
    return classes.split(" ").filter(function(className) {
      return className.trim().length > 1;
    });
  }, [classes]);
}
var TransitionContext = (0, import_react30.createContext)(null);
TransitionContext.displayName = "TransitionContext";
var TreeStates;
(function(TreeStates2) {
  TreeStates2["Visible"] = "visible";
  TreeStates2["Hidden"] = "hidden";
})(TreeStates || (TreeStates = {}));
function useTransitionContext() {
  var context = (0, import_react30.useContext)(TransitionContext);
  if (context === null) {
    throw new Error("A <Transition.Child /> is used but it is missing a parent <Transition /> or <Transition.Root />.");
  }
  return context;
}
function useParentNesting() {
  var context = (0, import_react30.useContext)(NestingContext);
  if (context === null) {
    throw new Error("A <Transition.Child /> is used but it is missing a parent <Transition /> or <Transition.Root />.");
  }
  return context;
}
var NestingContext = (0, import_react30.createContext)(null);
NestingContext.displayName = "NestingContext";
function hasChildren(bag) {
  if ("children" in bag)
    return hasChildren(bag.children);
  return bag.current.filter(function(_ref) {
    var state2 = _ref.state;
    return state2 === TreeStates.Visible;
  }).length > 0;
}
function useNesting(done) {
  var doneRef = (0, import_react30.useRef)(done);
  var transitionableChildren = (0, import_react30.useRef)([]);
  var mounted = useIsMounted();
  (0, import_react30.useEffect)(function() {
    doneRef.current = done;
  }, [done]);
  var unregister = (0, import_react30.useCallback)(function(childId, strategy) {
    var _match;
    if (strategy === void 0) {
      strategy = RenderStrategy.Hidden;
    }
    var idx = transitionableChildren.current.findIndex(function(_ref2) {
      var id2 = _ref2.id;
      return id2 === childId;
    });
    if (idx === -1)
      return;
    match(strategy, (_match = {}, _match[RenderStrategy.Unmount] = function() {
      transitionableChildren.current.splice(idx, 1);
    }, _match[RenderStrategy.Hidden] = function() {
      transitionableChildren.current[idx].state = TreeStates.Hidden;
    }, _match));
    if (!hasChildren(transitionableChildren) && mounted.current) {
      doneRef.current == null ? void 0 : doneRef.current();
    }
  }, [doneRef, mounted, transitionableChildren]);
  var register = (0, import_react30.useCallback)(function(childId) {
    var child = transitionableChildren.current.find(function(_ref3) {
      var id2 = _ref3.id;
      return id2 === childId;
    });
    if (!child) {
      transitionableChildren.current.push({
        id: childId,
        state: TreeStates.Visible
      });
    } else if (child.state !== TreeStates.Visible) {
      child.state = TreeStates.Visible;
    }
    return function() {
      return unregister(childId, RenderStrategy.Unmount);
    };
  }, [transitionableChildren, unregister]);
  return (0, import_react30.useMemo)(function() {
    return {
      children: transitionableChildren,
      register,
      unregister
    };
  }, [register, unregister, transitionableChildren]);
}
function noop() {
}
var eventNames = ["beforeEnter", "afterEnter", "beforeLeave", "afterLeave"];
function ensureEventHooksExist(events) {
  var result = {};
  for (var _iterator = _createForOfIteratorHelperLoose(eventNames), _step; !(_step = _iterator()).done; ) {
    var _events$name;
    var name = _step.value;
    result[name] = (_events$name = events[name]) != null ? _events$name : noop;
  }
  return result;
}
function useEvents(events) {
  var eventsRef = (0, import_react30.useRef)(ensureEventHooksExist(events));
  (0, import_react30.useEffect)(function() {
    eventsRef.current = ensureEventHooksExist(events);
  }, [events]);
  return eventsRef;
}
var DEFAULT_TRANSITION_CHILD_TAG = "div";
var TransitionChildRenderFeatures = Features.RenderStrategy;
function TransitionChild(props) {
  var _match3;
  var beforeEnter = props.beforeEnter, afterEnter = props.afterEnter, beforeLeave = props.beforeLeave, afterLeave = props.afterLeave, enter = props.enter, enterFrom = props.enterFrom, enterTo = props.enterTo, entered = props.entered, leave = props.leave, leaveFrom = props.leaveFrom, leaveTo = props.leaveTo, rest = _objectWithoutPropertiesLoose(props, ["beforeEnter", "afterEnter", "beforeLeave", "afterLeave", "enter", "enterFrom", "enterTo", "entered", "leave", "leaveFrom", "leaveTo"]);
  var container = (0, import_react30.useRef)(null);
  var _useState = (0, import_react30.useState)(TreeStates.Visible), state2 = _useState[0], setState = _useState[1];
  var strategy = rest.unmount ? RenderStrategy.Unmount : RenderStrategy.Hidden;
  var _useTransitionContext = useTransitionContext(), show = _useTransitionContext.show, appear = _useTransitionContext.appear, initial = _useTransitionContext.initial;
  var _useParentNesting = useParentNesting(), register = _useParentNesting.register, unregister = _useParentNesting.unregister;
  var id2 = useId();
  var isTransitioning = (0, import_react30.useRef)(false);
  var nesting = useNesting(function() {
    if (!isTransitioning.current) {
      setState(TreeStates.Hidden);
      unregister(id2);
      events.current.afterLeave();
    }
  });
  useIsoMorphicEffect(function() {
    if (!id2)
      return;
    return register(id2);
  }, [register, id2]);
  useIsoMorphicEffect(function() {
    var _match2;
    if (strategy !== RenderStrategy.Hidden)
      return;
    if (!id2)
      return;
    if (show && state2 !== TreeStates.Visible) {
      setState(TreeStates.Visible);
      return;
    }
    match(state2, (_match2 = {}, _match2[TreeStates.Hidden] = function() {
      return unregister(id2);
    }, _match2[TreeStates.Visible] = function() {
      return register(id2);
    }, _match2));
  }, [state2, id2, register, unregister, show, strategy]);
  var enterClasses = useSplitClasses(enter);
  var enterFromClasses = useSplitClasses(enterFrom);
  var enterToClasses = useSplitClasses(enterTo);
  var enteredClasses = useSplitClasses(entered);
  var leaveClasses = useSplitClasses(leave);
  var leaveFromClasses = useSplitClasses(leaveFrom);
  var leaveToClasses = useSplitClasses(leaveTo);
  var events = useEvents({
    beforeEnter,
    afterEnter,
    beforeLeave,
    afterLeave
  });
  var ready = useServerHandoffComplete();
  (0, import_react30.useEffect)(function() {
    if (ready && state2 === TreeStates.Visible && container.current === null) {
      throw new Error("Did you forget to passthrough the `ref` to the actual DOM node?");
    }
  }, [container, state2, ready]);
  var skip = initial && !appear;
  useIsoMorphicEffect(function() {
    var node = container.current;
    if (!node)
      return;
    if (skip)
      return;
    isTransitioning.current = true;
    if (show)
      events.current.beforeEnter();
    if (!show)
      events.current.beforeLeave();
    return show ? transition(node, enterClasses, enterFromClasses, enterToClasses, enteredClasses, function(reason) {
      isTransitioning.current = false;
      if (reason === Reason.Finished)
        events.current.afterEnter();
    }) : transition(node, leaveClasses, leaveFromClasses, leaveToClasses, enteredClasses, function(reason) {
      isTransitioning.current = false;
      if (reason !== Reason.Finished)
        return;
      if (!hasChildren(nesting)) {
        setState(TreeStates.Hidden);
        unregister(id2);
        events.current.afterLeave();
      }
    });
  }, [events, id2, isTransitioning, unregister, nesting, container, skip, show, enterClasses, enterFromClasses, enterToClasses, leaveClasses, leaveFromClasses, leaveToClasses]);
  var propsWeControl = {
    ref: container
  };
  var passthroughProps = rest;
  return import_react30.default.createElement(NestingContext.Provider, {
    value: nesting
  }, import_react30.default.createElement(OpenClosedProvider, {
    value: match(state2, (_match3 = {}, _match3[TreeStates.Visible] = State.Open, _match3[TreeStates.Hidden] = State.Closed, _match3))
  }, render({
    props: _extends({}, passthroughProps, propsWeControl),
    defaultTag: DEFAULT_TRANSITION_CHILD_TAG,
    features: TransitionChildRenderFeatures,
    visible: state2 === TreeStates.Visible,
    name: "Transition.Child"
  })));
}
function Transition(props) {
  var show = props.show, _props$appear = props.appear, appear = _props$appear === void 0 ? false : _props$appear, unmount = props.unmount, passthroughProps = _objectWithoutPropertiesLoose(props, ["show", "appear", "unmount"]);
  var usesOpenClosedState = useOpenClosed();
  if (show === void 0 && usesOpenClosedState !== null) {
    var _match4;
    show = match(usesOpenClosedState, (_match4 = {}, _match4[State.Open] = true, _match4[State.Closed] = false, _match4));
  }
  if (![true, false].includes(show)) {
    throw new Error("A <Transition /> is used but it is missing a `show={true | false}` prop.");
  }
  var _useState2 = (0, import_react30.useState)(show ? TreeStates.Visible : TreeStates.Hidden), state2 = _useState2[0], setState = _useState2[1];
  var nestingBag = useNesting(function() {
    setState(TreeStates.Hidden);
  });
  var initial = useIsInitialRender();
  var transitionBag = (0, import_react30.useMemo)(function() {
    return {
      show,
      appear: appear || !initial,
      initial
    };
  }, [show, appear, initial]);
  (0, import_react30.useEffect)(function() {
    if (show) {
      setState(TreeStates.Visible);
    } else if (!hasChildren(nestingBag)) {
      setState(TreeStates.Hidden);
    }
  }, [show, nestingBag]);
  var sharedProps = {
    unmount
  };
  return import_react30.default.createElement(NestingContext.Provider, {
    value: nestingBag
  }, import_react30.default.createElement(TransitionContext.Provider, {
    value: transitionBag
  }, render({
    props: _extends({}, sharedProps, {
      as: import_react30.Fragment,
      children: import_react30.default.createElement(TransitionChild, Object.assign({}, sharedProps, passthroughProps))
    }),
    defaultTag: import_react30.Fragment,
    features: TransitionChildRenderFeatures,
    visible: state2 === TreeStates.Visible,
    name: "Transition"
  })));
}
Transition.Child = function Child(props) {
  var hasTransitionContext = (0, import_react30.useContext)(TransitionContext) !== null;
  var hasOpenClosedContext = useOpenClosed() !== null;
  return !hasTransitionContext && hasOpenClosedContext ? import_react30.default.createElement(Transition, Object.assign({}, props)) : import_react30.default.createElement(TransitionChild, Object.assign({}, props));
};
Transition.Root = Transition;
export {
  Dialog2 as Dialog,
  Disclosure,
  FocusTrap,
  Listbox,
  Menu,
  Popover,
  Portal,
  RadioGroup,
  Switch,
  Tab,
  Transition
};
//# sourceMappingURL=@headlessui_react.js.map
